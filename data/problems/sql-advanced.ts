import type { FillProblem, OxProblem, PlanProblem } from '@/types/problem';

export const SQL_ADVANCED_PROBLEMS = [
  // ── avf01 ~ avf15: fill 문제 (계층형 쿼리 + CTE) ──────────────────
  {
    id: 'avf01',
    category: 'sql-advanced',
    difficulty: 'basic',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '직원 조직도 하향식 탐색',
    question: 'employees 테이블에서 emp_id=1인 직원 하위의 모든 직원을 계층 순서로 조회하려 한다. manager_id가 상위 직원을 가리킨다.',
    sqlTemplate: "SELECT emp_id, name, LEVEL FROM employees ___ WITH emp_id = 1 ___ PRIOR emp_id = manager_id;",
    blanks: 2,
    options: ['START', 'CONNECT BY', 'WHERE', 'ORDER BY', 'PRIOR', 'GROUP BY'],
    optionExplanations: [
      { value: 'START', explanation: 'START WITH는 계층 탐색의 시작 노드를 지정합니다' },
      { value: 'CONNECT BY', explanation: '부모-자식 관계를 정의하는 오라클 계층 쿼리 키워드입니다' },
      { value: 'WHERE', explanation: 'WHERE는 일반 행 필터링이며 계층 시작점 지정에 사용할 수 없습니다' },
      { value: 'ORDER BY', explanation: '정렬에 사용하며 계층 구조 정의와 무관합니다' },
      { value: 'PRIOR', explanation: 'CONNECT BY절 내에서 부모 행을 참조할 때 사용합니다' },
      { value: 'GROUP BY', explanation: '그룹화에 사용하며 계층 탐색과 관련 없습니다' },
    ],
    correctAnswers: ['START', 'CONNECT BY'],
    hints: {
      directional: ['오라클 계층 쿼리는 시작점 지정 + 부모-자식 관계 정의 두 부분으로 구성됩니다'],
      constraint: ['시작 노드는 START WITH, 관계 정의는 CONNECT BY PRIOR로 작성합니다'],
      misconception: ['WHERE절로는 계층 탐색 시작점을 지정할 수 없습니다. WHERE는 탐색 후 결과를 필터링합니다'],
    },
    learningPoint: '오라클 계층 쿼리: START WITH(시작점) + CONNECT BY PRIOR(관계 정의)',
    tags: ['CONNECT BY', 'START WITH', 'LEVEL'],
    explanation: '오라클 계층형 쿼리는 START WITH로 루트 노드를 지정하고, CONNECT BY PRIOR로 부모-자식 관계를 정의합니다. PRIOR emp_id = manager_id는 현재 행의 manager_id가 부모 행의 emp_id임을 의미합니다.',
    relatedConceptTags: ['sql-advanced'],
  } as FillProblem,
  {
    id: 'avf02',
    category: 'sql-advanced',
    difficulty: 'basic',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '상위 카테고리부터 하위까지 트리 전체 조회',
    question: 'categories 테이블에서 parent_id가 NULL인 최상위 카테고리부터 모든 하위 카테고리를 계층 순서로 조회하려 한다.',
    sqlTemplate: "SELECT category_id, name, LEVEL FROM categories START WITH ___ IS NULL CONNECT BY ___ category_id = parent_id;",
    blanks: 2,
    options: ['parent_id', 'PRIOR', 'category_id', 'name', 'LEVEL', 'CONNECT BY'],
    optionExplanations: [
      { value: 'parent_id', explanation: '최상위 카테고리는 parent_id가 NULL인 행입니다' },
      { value: 'PRIOR', explanation: '부모 행의 컬럼을 참조할 때 PRIOR 키워드를 사용합니다' },
      { value: 'category_id', explanation: 'category_id는 각 행의 고유 식별자이며, 시작 조건의 NULL 체크 대상이 아닙니다' },
      { value: 'name', explanation: '카테고리 이름은 계층 구조 정의와 무관합니다' },
      { value: 'LEVEL', explanation: 'LEVEL은 계층의 깊이를 나타내는 의사컬럼이지 관계 정의에 쓰이지 않습니다' },
      { value: 'CONNECT BY', explanation: 'CONNECT BY는 이미 절 앞에 작성되어 있으므로 빈칸에 들어갈 수 없습니다' },
    ],
    correctAnswers: ['parent_id', 'PRIOR'],
    hints: {
      directional: ['최상위 노드는 부모가 없는 행입니다. 어떤 컬럼이 NULL이면 루트일까요?'],
      constraint: ['CONNECT BY 뒤에서 부모 행을 참조하려면 PRIOR 키워드가 필요합니다'],
      misconception: ['PRIOR는 CONNECT BY절 안에서만 의미가 있으며, 부모 행의 컬럼 값을 가리킵니다'],
    },
    learningPoint: 'START WITH 조건이 IS NULL이면 부모가 없는 최상위 노드부터 탐색을 시작한다',
    tags: ['CONNECT BY', 'START WITH', 'PRIOR'],
    explanation: 'parent_id IS NULL은 최상위 카테고리를 의미합니다. CONNECT BY PRIOR category_id = parent_id는 부모 행의 category_id가 자식 행의 parent_id와 같은 관계를 정의합니다.',
    relatedConceptTags: ['sql-advanced'],
  } as FillProblem,
  {
    id: 'avf03',
    category: 'sql-advanced',
    difficulty: 'basic',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '단순 CTE로 배송 허브 목록 추출',
    question: 'WITH절을 사용하여 shipments 테이블에서 출발 허브(from_hub)가 "서울"인 배송 건을 hub_list라는 이름의 CTE로 정의한 뒤, 그 결과를 전부 조회하려 한다.',
    sqlTemplate: "___ hub_list AS (SELECT * FROM shipments WHERE from_hub = '서울') ___ * FROM hub_list;",
    blanks: 2,
    options: ['WITH', 'SELECT', 'FROM', 'CREATE VIEW', 'AS', 'WHERE'],
    optionExplanations: [
      { value: 'WITH', explanation: 'CTE를 정의하는 키워드입니다' },
      { value: 'SELECT', explanation: 'CTE 정의 후 본 쿼리에서 데이터를 조회하는 키워드입니다' },
      { value: 'FROM', explanation: 'FROM은 테이블/CTE를 지정하지만, 빈칸 위치에는 맞지 않습니다' },
      { value: 'CREATE VIEW', explanation: '뷰를 생성하는 DDL이며 CTE와 다릅니다. CTE는 일회성입니다' },
      { value: 'AS', explanation: 'AS는 CTE 이름 뒤에 서브쿼리를 연결할 때 쓰이지만 이미 작성되어 있습니다' },
      { value: 'WHERE', explanation: 'WHERE는 조건 필터링에 사용되며 CTE 정의 키워드가 아닙니다' },
    ],
    correctAnswers: ['WITH', 'SELECT'],
    hints: {
      directional: ['CTE는 "WITH 이름 AS (서브쿼리)" 형태로 정의하고, 뒤에 본 쿼리를 작성합니다'],
      constraint: ['CTE 정의가 끝난 후에는 일반적인 SELECT 문으로 결과를 조회합니다'],
      misconception: ['CREATE VIEW는 영구 객체를 만드는 DDL입니다. CTE는 WITH로 정의하는 일회성 임시 결과입니다'],
    },
    learningPoint: 'CTE(Common Table Expression)는 WITH 이름 AS (서브쿼리) 형태로 정의하고 바로 뒤 SELECT에서 사용한다',
    tags: ['WITH', 'CTE'],
    explanation: 'WITH절은 CTE를 정의하여 복잡한 쿼리를 가독성 있게 분리합니다. hub_list라는 이름으로 정의된 CTE는 바로 뒤의 SELECT문에서 테이블처럼 참조할 수 있습니다.',
    relatedConceptTags: ['sql-advanced'],
  } as FillProblem,
  {
    id: 'avf04',
    category: 'sql-advanced',
    difficulty: 'basic',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '특정 댓글의 모든 대댓글 조회',
    question: 'comments 테이블에서 comment_id=100인 댓글 아래의 모든 대댓글을 계층적으로 조회하려 한다. parent_id가 상위 댓글을 가리킨다.',
    sqlTemplate: "SELECT comment_id, content, LEVEL FROM comments START WITH comment_id = 100 ___ BY PRIOR ___ = parent_id;",
    blanks: 2,
    options: ['CONNECT', 'comment_id', 'ORDER', 'parent_id', 'GROUP', 'content'],
    optionExplanations: [
      { value: 'CONNECT', explanation: 'CONNECT BY는 계층 관계를 정의하는 오라클 전용 키워드입니다' },
      { value: 'comment_id', explanation: 'PRIOR 뒤에 오는 부모 행의 PK로, 자식의 parent_id와 매칭됩니다' },
      { value: 'ORDER', explanation: 'ORDER BY는 정렬에 사용하며 계층 관계 정의와 무관합니다' },
      { value: 'parent_id', explanation: 'parent_id는 자식 행의 FK이며, PRIOR 뒤가 아닌 등호 오른쪽에 이미 있습니다' },
      { value: 'GROUP', explanation: 'GROUP BY는 그룹화에 사용하며 계층 쿼리와 관련 없습니다' },
      { value: 'content', explanation: '댓글 내용은 계층 관계 정의에 사용할 수 없습니다' },
    ],
    correctAnswers: ['CONNECT', 'comment_id'],
    hints: {
      directional: ['계층 관계를 정의하는 절은 ___ BY PRIOR 형태입니다'],
      constraint: ['PRIOR 뒤에는 부모 행의 기본키가 와야 합니다'],
      misconception: ['PRIOR parent_id = comment_id로 쓰면 상향식(자식→부모) 탐색이 됩니다. 방향에 주의하세요'],
    },
    learningPoint: 'CONNECT BY PRIOR PK = FK는 하향식 탐색, PRIOR FK = PK는 상향식 탐색이다',
    tags: ['CONNECT BY', 'PRIOR', '대댓글'],
    explanation: 'CONNECT BY PRIOR comment_id = parent_id는 부모 행의 comment_id가 자식 행의 parent_id와 같은 관계를 정의합니다. START WITH comment_id = 100으로 특정 댓글을 루트로 지정하면 그 아래 모든 대댓글이 조회됩니다.',
    relatedConceptTags: ['sql-advanced'],
  } as FillProblem,
  {
    id: 'avf05',
    category: 'sql-advanced',
    difficulty: 'basic',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '승인 프로세스 시작 건 조회를 위한 CTE 정의',
    question: 'approvals 테이블에서 최초 승인 요청(parent_approval_id가 NULL)만 추출하는 CTE를 정의하고, 그중 status가 "대기"인 건을 조회하려 한다.',
    sqlTemplate: "WITH root_approvals ___ (SELECT * FROM approvals WHERE parent_approval_id IS NULL) SELECT * FROM root_approvals ___ status = '대기';",
    blanks: 2,
    options: ['AS', 'WHERE', 'FROM', 'HAVING', 'WITH', 'AND'],
    optionExplanations: [
      { value: 'AS', explanation: 'CTE 이름과 서브쿼리를 연결하는 키워드입니다' },
      { value: 'WHERE', explanation: '본 쿼리에서 CTE 결과를 필터링할 때 사용합니다' },
      { value: 'FROM', explanation: 'FROM은 이미 root_approvals 앞에 있으므로 빈칸에 맞지 않습니다' },
      { value: 'HAVING', explanation: 'HAVING은 GROUP BY 뒤에서 그룹 조건을 거는 키워드입니다' },
      { value: 'WITH', explanation: 'WITH는 CTE 정의 시작에 사용하며 이미 앞에 작성되어 있습니다' },
      { value: 'AND', explanation: 'AND는 조건을 연결하지만, 새로운 조건절을 시작하지는 않습니다' },
    ],
    correctAnswers: ['AS', 'WHERE'],
    hints: {
      directional: ['CTE 이름 뒤에 서브쿼리를 연결하는 키워드와, 본 쿼리의 필터 키워드를 찾으세요'],
      constraint: ['WITH 이름 ___(...) 형태에서 빈칸에 들어갈 연결 키워드를 생각해보세요'],
      misconception: ['HAVING은 GROUP BY와 함께 사용하는 것이며, 단순 행 필터링에는 WHERE를 씁니다'],
    },
    learningPoint: 'CTE 정의는 WITH 이름 AS (서브쿼리) 형태이며, 본 쿼리에서 WHERE로 추가 필터링이 가능하다',
    tags: ['WITH', 'CTE', 'WHERE'],
    explanation: 'WITH root_approvals AS (...) 구문으로 CTE를 정의하고, 바로 뒤의 SELECT문에서 WHERE절로 추가 조건을 걸 수 있습니다. CTE는 가독성을 높이고 복잡한 쿼리를 단계적으로 분리합니다.',
    relatedConceptTags: ['sql-advanced'],
  } as FillProblem,
  {
    id: 'avf06',
    category: 'sql-advanced',
    difficulty: 'basic',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '배송 경로의 단계별 깊이 조회',
    question: 'shipments 테이블에서 shipment_id=1인 최초 배송부터 이어지는 모든 중계 경로를 조회하되, 각 구간이 몇 번째 중계인지 LEVEL로 표시하려 한다.',
    sqlTemplate: "SELECT shipment_id, from_hub, to_hub, ___ FROM shipments START WITH shipment_id = 1 CONNECT BY PRIOR shipment_id = ___;",
    blanks: 2,
    options: ['LEVEL', 'parent_shipment_id', 'ROWNUM', 'shipment_id', 'from_hub', 'to_hub'],
    optionExplanations: [
      { value: 'LEVEL', explanation: '계층 쿼리에서 현재 행의 깊이(루트=1)를 나타내는 의사컬럼입니다' },
      { value: 'parent_shipment_id', explanation: '자식 행이 부모를 참조하는 FK이며, CONNECT BY 등호 오른쪽에 위치합니다' },
      { value: 'ROWNUM', explanation: 'ROWNUM은 결과 행 번호이며 계층 깊이와 무관합니다' },
      { value: 'shipment_id', explanation: 'shipment_id는 PK이며, PRIOR 뒤에 이미 사용되고 있습니다' },
      { value: 'from_hub', explanation: '출발 허브 이름이며 계층 깊이를 나타내지 않습니다' },
      { value: 'to_hub', explanation: '도착 허브 이름이며 계층 깊이를 나타내지 않습니다' },
    ],
    correctAnswers: ['LEVEL', 'parent_shipment_id'],
    hints: {
      directional: ['계층 깊이를 나타내는 오라클 의사컬럼이 있습니다'],
      constraint: ['CONNECT BY PRIOR PK = FK 형태에서 FK 컬럼이 빈칸에 들어갑니다'],
      misconception: ['ROWNUM은 단순히 결과 행 번호일 뿐이며, 계층 구조의 깊이를 나타내지 않습니다'],
    },
    learningPoint: 'LEVEL 의사컬럼은 계층 쿼리에서 루트(1)부터 현재 행까지의 깊이를 반환한다',
    tags: ['LEVEL', 'CONNECT BY', '배송경로'],
    explanation: 'LEVEL은 오라클 계층 쿼리 전용 의사컬럼으로, 루트 노드가 1이고 하위로 갈수록 1씩 증가합니다. CONNECT BY PRIOR shipment_id = parent_shipment_id로 부모-자식 관계를 정의합니다.',
    relatedConceptTags: ['sql-advanced'],
  } as FillProblem,
  {
    id: 'avf07',
    category: 'sql-advanced',
    difficulty: 'intermediate',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '조직도에서 특정 부서 직원만 계층 탐색',
    question: 'employees 테이블에서 dept가 "개발팀"인 직원 중 매니저가 없는(manager_id IS NULL) 직원을 루트로 하여, 3단계(LEVEL <= 3)까지만 조회하려 한다.',
    sqlTemplate: "SELECT emp_id, name, LEVEL FROM employees START WITH manager_id IS NULL AND dept = '개발팀' CONNECT BY PRIOR emp_id = manager_id AND ___ <= ___;",
    blanks: 2,
    options: ['LEVEL', '3', 'ROWNUM', '2', 'DEPTH', '5'],
    optionExplanations: [
      { value: 'LEVEL', explanation: '계층 깊이를 나타내는 의사컬럼으로, 탐색 깊이를 제한할 때 사용합니다' },
      { value: '3', explanation: '3단계까지 조회하라는 조건이므로 상한값은 3입니다' },
      { value: 'ROWNUM', explanation: 'ROWNUM은 결과 행 번호이며, 계층 깊이 제한에는 LEVEL을 사용합니다' },
      { value: '2', explanation: '문제 조건은 3단계까지이므로 2는 범위가 부족합니다' },
      { value: 'DEPTH', explanation: 'DEPTH는 오라클 계층 쿼리에서 사용하지 않는 키워드입니다' },
      { value: '5', explanation: '문제 조건이 3단계까지이므로 5는 범위를 초과합니다' },
    ],
    correctAnswers: ['LEVEL', '3'],
    hints: {
      directional: ['CONNECT BY절에 추가 조건을 걸어 탐색 깊이를 제한할 수 있습니다'],
      constraint: ['AND LEVEL <= N 형태로 계층 깊이를 제한합니다'],
      misconception: ['WHERE LEVEL <= 3은 탐색 자체를 제한하지 않고 결과만 필터링합니다. CONNECT BY절에 넣어야 탐색을 멈춥니다'],
    },
    learningPoint: 'CONNECT BY절에 LEVEL 조건을 추가하면 탐색 깊이 자체를 제한할 수 있다',
    tags: ['CONNECT BY', 'LEVEL', 'START WITH'],
    explanation: 'CONNECT BY PRIOR emp_id = manager_id AND LEVEL <= 3은 3단계까지만 계층 탐색을 수행합니다. WHERE절에 LEVEL 조건을 넣으면 탐색은 전체를 수행한 후 결과만 필터링하므로 성능상 차이가 있습니다.',
    relatedConceptTags: ['sql-advanced'],
  } as FillProblem,
  {
    id: 'avf08',
    category: 'sql-advanced',
    difficulty: 'intermediate',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '카테고리 트리 상향식 탐색 (리프→루트)',
    question: 'categories 테이블에서 category_id=50인 카테고리부터 최상위 부모까지 역방향으로 올라가며 조회하려 한다. parent_id가 상위 카테고리를 가리킨다.',
    sqlTemplate: "SELECT category_id, name, LEVEL FROM categories START WITH category_id = 50 CONNECT BY PRIOR ___ = ___;",
    blanks: 2,
    options: ['parent_id', 'category_id', 'name', 'LEVEL', 'PRIOR', 'ROWNUM'],
    optionExplanations: [
      { value: 'parent_id', explanation: '상향식 탐색에서 PRIOR 뒤에 FK(parent_id)를 놓으면 부모 방향으로 이동합니다' },
      { value: 'category_id', explanation: '등호 오른쪽에 PK를 놓으면 부모 행과 매칭됩니다' },
      { value: 'name', explanation: '카테고리 이름은 관계 정의에 사용할 수 없습니다' },
      { value: 'LEVEL', explanation: 'LEVEL은 깊이를 나타내는 의사컬럼이며 관계 정의에 사용하지 않습니다' },
      { value: 'PRIOR', explanation: 'PRIOR는 이미 CONNECT BY 뒤에 있으므로 빈칸에 중복 사용하지 않습니다' },
      { value: 'ROWNUM', explanation: 'ROWNUM은 행 번호이며 계층 관계 정의와 무관합니다' },
    ],
    correctAnswers: ['parent_id', 'category_id'],
    hints: {
      directional: ['상향식 탐색은 PRIOR 뒤에 FK를 놓아 부모 방향으로 이동합니다'],
      constraint: ['PRIOR parent_id = category_id는 "현재 행의 parent_id가 다음 행의 category_id"라는 의미입니다'],
      misconception: ['PRIOR category_id = parent_id로 쓰면 하향식(부모→자식) 탐색이 됩니다. 방향이 반대입니다'],
    },
    learningPoint: 'PRIOR FK = PK (예: PRIOR parent_id = category_id)는 자식→부모 상향식 탐색이다',
    tags: ['CONNECT BY', 'PRIOR', '상향식'],
    explanation: 'PRIOR parent_id = category_id는 현재 행의 parent_id 값으로 다음 행의 category_id를 찾아 올라갑니다. 하향식은 PRIOR PK = FK, 상향식은 PRIOR FK = PK로 방향이 결정됩니다.',
    relatedConceptTags: ['sql-advanced'],
  } as FillProblem,
  {
    id: 'avf09',
    category: 'sql-advanced',
    difficulty: 'intermediate',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '재귀 CTE로 댓글 트리 전개',
    question: 'WITH RECURSIVE를 사용하여 comment_id=1인 댓글부터 모든 대댓글을 재귀적으로 조회하려 한다. 앵커 멤버와 재귀 멤버를 UNION ALL로 연결한다.',
    sqlTemplate: "WITH ___ comment_tree AS (SELECT comment_id, content, parent_id, 1 AS depth FROM comments WHERE comment_id = 1 UNION ALL SELECT c.comment_id, c.content, c.parent_id, ct.depth + 1 FROM comments c JOIN comment_tree ct ON c.parent_id = ct.comment_id) SELECT * FROM ___;",
    blanks: 2,
    options: ['RECURSIVE', 'comment_tree', 'comments', 'CONNECT BY', 'TEMPORARY', 'depth'],
    optionExplanations: [
      { value: 'RECURSIVE', explanation: '표준 SQL에서 재귀 CTE를 정의할 때 WITH 뒤에 RECURSIVE 키워드를 붙입니다' },
      { value: 'comment_tree', explanation: '정의한 CTE 이름을 본 쿼리의 FROM절에서 참조합니다' },
      { value: 'comments', explanation: 'comments는 원본 테이블이며, CTE 결과를 조회하려면 CTE 이름을 사용해야 합니다' },
      { value: 'CONNECT BY', explanation: 'CONNECT BY는 오라클 전용 구문이며 표준 SQL 재귀 CTE와 함께 사용하지 않습니다' },
      { value: 'TEMPORARY', explanation: 'TEMPORARY는 임시 테이블 생성에 사용하는 키워드이며 CTE와 무관합니다' },
      { value: 'depth', explanation: 'depth는 SELECT절의 별칭이며 CTE 정의 키워드가 아닙니다' },
    ],
    correctAnswers: ['RECURSIVE', 'comment_tree'],
    hints: {
      directional: ['표준 SQL 재귀 CTE는 WITH 뒤에 특별한 키워드가 필요합니다'],
      constraint: ['본 쿼리의 FROM절에는 CTE 이름을 적어 재귀 결과를 조회합니다'],
      misconception: ['CONNECT BY는 오라클 전용이며, 표준 SQL에서는 WITH RECURSIVE로 재귀를 구현합니다'],
    },
    learningPoint: '표준 SQL 재귀 CTE는 WITH RECURSIVE 이름 AS (앵커 UNION ALL 재귀) 형태로 작성한다',
    tags: ['WITH RECURSIVE', 'CTE', 'UNION ALL'],
    explanation: 'WITH RECURSIVE는 표준 SQL에서 계층 데이터를 탐색하는 방법입니다. 앵커 멤버(시작점)와 재귀 멤버(반복)를 UNION ALL로 결합하며, 재귀 멤버에서 CTE 자신을 참조합니다.',
    relatedConceptTags: ['sql-advanced'],
  } as FillProblem,
  {
    id: 'avf10',
    category: 'sql-advanced',
    difficulty: 'intermediate',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '다중 CTE로 승인 현황 요약',
    question: 'WITH절에서 두 개의 CTE를 정의하려 한다. 첫 번째 CTE pending_list는 status가 "대기"인 승인 건, 두 번째 CTE approved_list는 status가 "승인"인 건을 추출한 뒤, 두 CTE를 request_id로 JOIN한다.',
    sqlTemplate: "WITH pending_list AS (SELECT * FROM approvals WHERE status = '대기')___ approved_list AS (SELECT * FROM approvals WHERE status = '승인') SELECT p.request_id, a.approver_id FROM pending_list p ___ approved_list a ON p.request_id = a.request_id;",
    blanks: 2,
    options: [', ', 'JOIN', 'WITH', 'UNION ALL', 'LEFT JOIN', 'AND'],
    optionExplanations: [
      { value: ', ', explanation: '다중 CTE는 쉼표(,)로 구분하여 나열합니다' },
      { value: 'JOIN', explanation: '본 쿼리에서 두 CTE를 결합할 때 JOIN을 사용합니다' },
      { value: 'WITH', explanation: 'WITH는 CTE 정의 시작에 한 번만 사용합니다. 두 번째 CTE에는 쉼표로 연결합니다' },
      { value: 'UNION ALL', explanation: 'UNION ALL은 결과를 세로로 합치며, CTE 구분 용도가 아닙니다' },
      { value: 'LEFT JOIN', explanation: 'LEFT JOIN도 가능하지만, 문제에서는 두 CTE 모두 일치하는 건만 필요합니다' },
      { value: 'AND', explanation: 'AND는 조건 연결에 사용하며 CTE 구분에 사용하지 않습니다' },
    ],
    correctAnswers: [', ', 'JOIN'],
    hints: {
      directional: ['WITH절에서 여러 CTE를 정의할 때 WITH를 반복하지 않습니다'],
      constraint: ['다중 CTE는 쉼표(,)로 구분하며, 본 쿼리에서 일반 JOIN처럼 사용합니다'],
      misconception: ['WITH를 두 번 쓰면 구문 오류가 발생합니다. 쉼표로 CTE를 이어서 정의하세요'],
    },
    learningPoint: '다중 CTE는 WITH를 한 번만 쓰고, 각 CTE를 쉼표(,)로 구분하여 나열한다',
    tags: ['WITH', 'CTE', 'JOIN'],
    explanation: '여러 CTE를 정의할 때 WITH는 한 번만 작성하고, 각 CTE 사이에 쉼표를 넣어 구분합니다. 정의된 CTE는 본 쿼리에서 일반 테이블처럼 JOIN할 수 있습니다.',
    relatedConceptTags: ['sql-advanced'],
  } as FillProblem,
  {
    id: 'avf11',
    category: 'sql-advanced',
    difficulty: 'intermediate',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '조직도에서 직속 부하만 필터링 (LEVEL 활용)',
    question: 'employees 테이블에서 emp_id=1인 사장의 직속 부하(LEVEL=2)만 조회하려 한다. 계층 전체를 탐색한 뒤 결과를 필터링한다.',
    sqlTemplate: "SELECT emp_id, name FROM employees ___ WITH emp_id = 1 CONNECT BY PRIOR emp_id = manager_id AND LEVEL <= 2 WHERE ___ = 2;",
    blanks: 2,
    options: ['START', 'LEVEL', 'WHERE', 'DEPTH', 'ROWNUM', 'CONNECT BY'],
    optionExplanations: [
      { value: 'START', explanation: 'START WITH는 계층 탐색의 시작 노드를 지정합니다' },
      { value: 'LEVEL', explanation: 'WHERE절에서 LEVEL을 사용하면 특정 깊이의 행만 필터링할 수 있습니다' },
      { value: 'WHERE', explanation: 'WHERE는 이미 절 키워드로 사용되었으며, 빈칸에는 컬럼/의사컬럼이 필요합니다' },
      { value: 'DEPTH', explanation: 'DEPTH는 오라클 계층 쿼리에서 유효한 의사컬럼이 아닙니다' },
      { value: 'ROWNUM', explanation: 'ROWNUM은 행 번호이며 계층 깊이를 나타내지 않습니다' },
      { value: 'CONNECT BY', explanation: 'CONNECT BY는 이미 관계 정의에 사용되었습니다' },
    ],
    correctAnswers: ['START', 'LEVEL'],
    hints: {
      directional: ['계층 탐색 시작점과 결과 필터링에 사용하는 의사컬럼을 구분하세요'],
      constraint: ['LEVEL=2는 루트(사장)로부터 한 단계 아래, 즉 직속 부하를 의미합니다'],
      misconception: ['LEVEL=1이 루트(시작점)이고, LEVEL=2가 직속 부하입니다. 0부터 시작하지 않습니다'],
    },
    learningPoint: 'LEVEL 의사컬럼은 1부터 시작하며, WHERE LEVEL = N으로 특정 깊이만 필터링할 수 있다',
    tags: ['LEVEL', 'CONNECT BY', 'WHERE'],
    explanation: 'LEVEL=1은 START WITH로 지정한 루트 노드이고, LEVEL=2는 루트의 직속 자식입니다. WHERE LEVEL = 2를 사용하면 전체 탐색 결과에서 직속 부하만 추출할 수 있습니다.',
    relatedConceptTags: ['sql-advanced'],
  } as FillProblem,
  {
    id: 'avf12',
    category: 'sql-advanced',
    difficulty: 'intermediate',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '배송 경로를 CTE로 집계하여 허브별 건수 조회',
    question: 'WITH절로 shipments에서 출발 허브(from_hub)별 배송 건수를 집계하는 CTE hub_stats를 정의한 뒤, 건수가 10 이상인 허브만 조회하려 한다.',
    sqlTemplate: "WITH hub_stats AS (SELECT from_hub, ___(shipment_id) AS cnt FROM shipments GROUP BY from_hub) SELECT from_hub, cnt FROM hub_stats WHERE cnt ___ 10;",
    blanks: 2,
    options: ['COUNT', '>=', 'SUM', '>', 'AVG', '='],
    optionExplanations: [
      { value: 'COUNT', explanation: '건수를 세는 집계 함수이며, 배송 건수 집계에 적합합니다' },
      { value: '>=', explanation: '10 이상이라는 조건은 >= 연산자로 표현합니다' },
      { value: 'SUM', explanation: 'SUM은 합계를 구하며, 건수 집계에는 COUNT가 적합합니다' },
      { value: '>', explanation: '> 10은 10 초과(11부터)이며, 10 이상과는 다릅니다' },
      { value: 'AVG', explanation: 'AVG는 평균을 구하는 함수이며 건수 집계와 무관합니다' },
      { value: '=', explanation: '= 10은 정확히 10인 경우만 해당하며 "이상"과 다릅니다' },
    ],
    correctAnswers: ['COUNT', '>='],
    hints: {
      directional: ['배송 건수를 세려면 어떤 집계 함수를 사용해야 할까요?'],
      constraint: ['"10 이상"은 10을 포함하는 비교 연산자를 사용해야 합니다'],
      misconception: ['> 10은 10을 포함하지 않습니다(11부터). "이상"은 >= 입니다'],
    },
    learningPoint: 'CTE 내부에서 GROUP BY + 집계 함수를 사용하면 본 쿼리에서 간결하게 필터링할 수 있다',
    tags: ['WITH', 'CTE', 'COUNT', 'GROUP BY'],
    explanation: 'CTE 안에서 GROUP BY와 COUNT로 집계를 수행하고, 본 쿼리에서 WHERE로 필터링하면 HAVING을 쓰지 않아도 가독성 좋은 쿼리를 작성할 수 있습니다. cnt >= 10은 10 이상을 의미합니다.',
    relatedConceptTags: ['sql-advanced'],
  } as FillProblem,
  {
    id: 'avf13',
    category: 'sql-advanced',
    difficulty: 'advanced',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '조직도에서 최상위 임원 이름 추적 (CONNECT_BY_ROOT)',
    question: 'employees 테이블에서 모든 직원에 대해, 해당 직원이 속한 계층의 최상위 루트 직원 이름을 함께 조회하려 한다. 오라클 CONNECT_BY_ROOT를 사용한다.',
    sqlTemplate: "SELECT emp_id, name, ___ name AS root_name, LEVEL FROM employees START WITH manager_id IS NULL CONNECT BY PRIOR emp_id = ___;",
    blanks: 2,
    options: ['CONNECT_BY_ROOT', 'manager_id', 'SYS_CONNECT_BY_PATH', 'emp_id', 'PRIOR', 'LEVEL'],
    optionExplanations: [
      { value: 'CONNECT_BY_ROOT', explanation: '계층 쿼리에서 루트 행의 컬럼 값을 반환하는 오라클 전용 연산자입니다' },
      { value: 'manager_id', explanation: '자식 행의 FK이며, CONNECT BY PRIOR PK = FK 형태에서 등호 오른쪽에 위치합니다' },
      { value: 'SYS_CONNECT_BY_PATH', explanation: '경로 전체를 문자열로 반환하는 함수이며, 루트 값만 가져오는 용도가 아닙니다' },
      { value: 'emp_id', explanation: 'emp_id는 PK이며, 이미 PRIOR 뒤에서 사용됩니다' },
      { value: 'PRIOR', explanation: 'PRIOR는 부모 행 참조 키워드이며, 루트 행 참조에는 CONNECT_BY_ROOT를 사용합니다' },
      { value: 'LEVEL', explanation: 'LEVEL은 깊이를 나타내며 루트 행의 값을 가져오지 않습니다' },
    ],
    correctAnswers: ['CONNECT_BY_ROOT', 'manager_id'],
    hints: {
      directional: ['루트 행의 특정 컬럼 값을 반환하는 오라클 전용 연산자가 있습니다'],
      constraint: ['CONNECT_BY_ROOT 컬럼명 형태로 사용하며, 해당 계층의 시작(루트) 행 값을 반환합니다'],
      misconception: ['PRIOR는 바로 위 부모 행을 참조하지만, CONNECT_BY_ROOT는 최상위 루트 행을 참조합니다'],
    },
    learningPoint: 'CONNECT_BY_ROOT 컬럼명은 현재 행이 속한 계층의 최상위 루트 행 값을 반환한다',
    tags: ['CONNECT_BY_ROOT', 'CONNECT BY', '계층쿼리'],
    explanation: 'CONNECT_BY_ROOT name은 현재 행이 어떤 계층 트리에 속하든, 해당 트리의 루트 행의 name 값을 반환합니다. PRIOR가 바로 위 부모를 참조하는 것과 달리, CONNECT_BY_ROOT는 최상위까지 올라갑니다.',
    relatedConceptTags: ['sql-advanced'],
  } as FillProblem,
  {
    id: 'avf14',
    category: 'sql-advanced',
    difficulty: 'advanced',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '카테고리 경로를 문자열로 출력 (SYS_CONNECT_BY_PATH)',
    question: 'categories 테이블에서 최상위 카테고리부터 현재 카테고리까지의 전체 경로를 "/"로 구분된 문자열로 출력하려 한다. 예: /전자제품/컴퓨터/노트북',
    sqlTemplate: "SELECT category_id, name, ___(name, ___) AS full_path FROM categories START WITH parent_id IS NULL CONNECT BY PRIOR category_id = parent_id;",
    blanks: 2,
    options: ['SYS_CONNECT_BY_PATH', "'/'", 'CONNECT_BY_ROOT', "'-'", 'LISTAGG', "'>'"],
    optionExplanations: [
      { value: 'SYS_CONNECT_BY_PATH', explanation: '루트부터 현재 행까지의 경로를 구분자로 연결한 문자열을 반환합니다' },
      { value: "'/'", explanation: '경로 구분자로 슬래시(/)를 사용하면 /전자제품/컴퓨터/노트북 형태가 됩니다' },
      { value: 'CONNECT_BY_ROOT', explanation: 'CONNECT_BY_ROOT는 루트 행의 단일 값만 반환하며 전체 경로를 만들지 않습니다' },
      { value: "'-'", explanation: '하이픈 구분자도 가능하지만, 문제에서 요구하는 형태는 /로 구분됩니다' },
      { value: 'LISTAGG', explanation: 'LISTAGG는 그룹 내 여러 행의 값을 연결하는 함수이며, 계층 경로용이 아닙니다' },
      { value: "'>'", explanation: '> 구분자도 가능하지만, 문제에서 요구하는 형태는 /로 구분됩니다' },
    ],
    correctAnswers: ['SYS_CONNECT_BY_PATH', "'/'"],
    hints: {
      directional: ['루트부터 현재까지의 경로를 문자열로 만드는 오라클 전용 함수가 있습니다'],
      constraint: ['SYS_CONNECT_BY_PATH(컬럼, 구분자) 형태이며, 구분자는 문자열 리터럴입니다'],
      misconception: ['LISTAGG는 같은 그룹의 여러 행을 수평으로 합치지만, 계층 경로와는 다른 개념입니다'],
    },
    learningPoint: 'SYS_CONNECT_BY_PATH(컬럼, 구분자)는 루트→현재 행까지 경로를 구분자로 연결한 문자열을 반환한다',
    tags: ['SYS_CONNECT_BY_PATH', 'CONNECT BY', '경로'],
    explanation: 'SYS_CONNECT_BY_PATH(name, \'/\')는 루트부터 현재 행까지 name 값을 /로 연결한 문자열을 반환합니다. 결과는 /전자제품/컴퓨터/노트북처럼 항상 구분자로 시작합니다.',
    relatedConceptTags: ['sql-advanced'],
  } as FillProblem,
  {
    id: 'avf15',
    category: 'sql-advanced',
    difficulty: 'advanced',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '재귀 CTE로 승인 체인 전체 경로와 깊이 추적',
    question: 'approvals 테이블에서 approval_id=1인 최초 승인부터 후속 승인까지 전체 체인을 재귀 CTE로 조회하려 한다. 각 승인의 깊이(depth)와 경로(path)를 함께 구한다.',
    sqlTemplate: "WITH RECURSIVE approval_chain AS (SELECT approval_id, approver_id, status, 1 AS depth, CAST(approval_id AS VARCHAR(200)) AS path FROM approvals WHERE approval_id = 1 ___ ___ SELECT a.approval_id, a.approver_id, a.status, ac.depth + 1, ac.path || '>' || CAST(a.approval_id AS VARCHAR(200)) FROM approvals a JOIN approval_chain ac ON a.parent_approval_id = ac.approval_id) SELECT * FROM approval_chain;",
    blanks: 2,
    options: ['UNION ALL', 'SELECT', 'UNION', 'INSERT', 'CONNECT BY', 'RECURSIVE'],
    optionExplanations: [
      { value: 'UNION ALL', explanation: '재귀 CTE에서 앵커 멤버와 재귀 멤버를 연결하는 연산자입니다' },
      { value: 'SELECT', explanation: '재귀 멤버의 SELECT문을 시작하는 키워드입니다' },
      { value: 'UNION', explanation: 'UNION은 중복 제거를 수행하므로 재귀 CTE에서는 보통 UNION ALL을 사용합니다' },
      { value: 'INSERT', explanation: 'INSERT는 데이터 삽입 DML이며 CTE 정의에 사용하지 않습니다' },
      { value: 'CONNECT BY', explanation: 'CONNECT BY는 오라클 전용 계층 쿼리이며, 재귀 CTE와 함께 사용하지 않습니다' },
      { value: 'RECURSIVE', explanation: 'RECURSIVE는 WITH 뒤에 한 번만 사용하며, 이미 작성되어 있습니다' },
    ],
    correctAnswers: ['UNION ALL', 'SELECT'],
    hints: {
      directional: ['재귀 CTE에서 앵커 멤버와 재귀 멤버를 어떤 연산자로 연결할까요?'],
      constraint: ['UNION ALL 뒤에는 재귀 멤버의 SELECT문이 이어집니다'],
      misconception: ['UNION은 중복 제거 비용이 발생하고 재귀 탐색 시 무한루프 방지에 도움이 되지 않습니다. 재귀 CTE에서는 UNION ALL이 표준입니다'],
    },
    learningPoint: '재귀 CTE는 앵커(시작점) UNION ALL 재귀(반복) 구조이며, 경로 추적에는 문자열 연결을 사용한다',
    tags: ['WITH RECURSIVE', 'CTE', 'UNION ALL', '경로추적'],
    explanation: '재귀 CTE에서 앵커 멤버(시작점)와 재귀 멤버(반복 부분)는 UNION ALL로 연결합니다. 재귀 멤버의 SELECT에서 CTE 자신(approval_chain)을 JOIN하여 다음 단계를 찾고, 경로는 문자열 연결(||)로 추적합니다.',
    relatedConceptTags: ['sql-advanced'],
  } as FillProblem,

  // ── avo01 ~ avo08: OX 문제 (계층형 쿼리 + CTE) ──────────────────
  {
    id: 'avo01',
    category: 'sql-advanced',
    difficulty: 'basic',
    type: 'ox',
    availableModes: ['write', 'fill'] as const,
    title: 'CONNECT BY의 SQL 표준 여부',
    question: 'CONNECT BY가 표준 SQL에 포함되는지 확인합니다.',
    statement: 'CONNECT BY는 표준 SQL 문법이다.',
    answer: 'X',
    hints: {
      directional: ['CONNECT BY를 지원하는 DBMS가 얼마나 되는지 생각해보세요'],
      misconception: ['CONNECT BY는 오라클 전용 구문입니다. 표준 SQL에서는 WITH RECURSIVE를 사용합니다'],
    },
    learningPoint: 'CONNECT BY는 오라클 전용이며, 표준 SQL 계층 쿼리는 WITH RECURSIVE이다',
    tags: ['CONNECT BY', '표준SQL'],
    explanation: 'CONNECT BY는 오라클에서 독자적으로 제공하는 계층형 쿼리 구문입니다. ISO SQL 표준에서는 재귀 CTE(WITH RECURSIVE)를 사용하여 계층 데이터를 처리합니다. PostgreSQL, MySQL 8.0+, SQL Server 등은 표준 재귀 CTE를 지원합니다.',
    relatedConceptTags: ['sql-advanced'],
  } as OxProblem,
  {
    id: 'avo02',
    category: 'sql-advanced',
    difficulty: 'basic',
    type: 'ox',
    availableModes: ['write', 'fill'] as const,
    title: 'START WITH 생략 시 동작',
    question: 'START WITH절을 생략했을 때 계층 쿼리의 동작 방식을 확인합니다.',
    statement: 'START WITH를 생략하면 모든 행이 루트가 된다.',
    answer: 'O',
    hints: {
      directional: ['START WITH가 루트 노드를 제한하는 역할임을 생각해보세요'],
      misconception: ['START WITH를 생략하면 오류가 발생하는 것이 아니라, 테이블의 모든 행을 시작점으로 탐색합니다'],
    },
    learningPoint: 'START WITH를 생략하면 모든 행이 루트가 되어 각 행부터 하위 탐색이 수행된다',
    tags: ['START WITH', 'CONNECT BY'],
    explanation: 'START WITH절은 계층 탐색의 시작 노드를 지정합니다. 이를 생략하면 테이블의 모든 행이 루트 노드로 간주되어, 각 행에서부터 하위 계층 탐색이 시작됩니다. 결과적으로 중복된 행이 여러 번 출력될 수 있습니다.',
    relatedConceptTags: ['sql-advanced'],
  } as OxProblem,
  {
    id: 'avo03',
    category: 'sql-advanced',
    difficulty: 'basic',
    type: 'ox',
    availableModes: ['write', 'fill'] as const,
    title: 'LEVEL 의사컬럼의 시작값',
    question: 'LEVEL 의사컬럼이 루트 노드에서 어떤 값을 갖는지 확인합니다.',
    statement: 'LEVEL은 루트 노드에서 1부터 시작한다.',
    answer: 'O',
    hints: {
      directional: ['오라클 계층 쿼리에서 루트 노드의 LEVEL 값을 떠올려보세요'],
      misconception: ['프로그래밍 언어의 인덱스(0부터)와 달리, LEVEL은 1부터 시작합니다'],
    },
    learningPoint: 'LEVEL 의사컬럼은 루트 노드가 1이며, 한 단계 내려갈 때마다 1씩 증가한다',
    tags: ['LEVEL', 'CONNECT BY'],
    explanation: 'LEVEL 의사컬럼은 START WITH로 지정된 루트 노드에서 1부터 시작합니다. 자식 노드는 2, 손자 노드는 3이 됩니다. 0부터 시작하지 않으므로 주의가 필요합니다.',
    relatedConceptTags: ['sql-advanced'],
  } as OxProblem,
  {
    id: 'avo04',
    category: 'sql-advanced',
    difficulty: 'intermediate',
    type: 'ox',
    availableModes: ['write', 'fill'] as const,
    title: 'CTE의 다중 참조 가능 여부',
    question: 'WITH절로 정의한 CTE를 같은 쿼리 내에서 여러 번 참조할 수 있는지 확인합니다.',
    statement: 'WITH절 CTE는 같은 쿼리에서 여러 번 참조할 수 있다.',
    answer: 'O',
    hints: {
      directional: ['CTE의 장점 중 하나가 재사용성임을 생각해보세요'],
      misconception: ['서브쿼리와 달리 CTE는 이름을 붙여 정의하므로, 본 쿼리에서 여러 번 참조가 가능합니다'],
    },
    learningPoint: 'CTE는 한 번 정의하면 같은 쿼리 내에서 여러 번 참조할 수 있어 서브쿼리보다 가독성이 좋다',
    tags: ['WITH', 'CTE', '다중참조'],
    explanation: 'WITH절로 정의한 CTE는 본 쿼리 내에서 테이블처럼 여러 번 참조할 수 있습니다. 이것이 인라인 서브쿼리 대비 CTE의 주요 장점입니다. 같은 서브쿼리를 여러 번 작성할 필요 없이 CTE 이름으로 재사용합니다.',
    relatedConceptTags: ['sql-advanced'],
  } as OxProblem,
  {
    id: 'avo05',
    category: 'sql-advanced',
    difficulty: 'intermediate',
    type: 'ox',
    availableModes: ['write', 'fill'] as const,
    title: 'CONNECT_BY_ROOT의 반환 대상',
    question: 'CONNECT_BY_ROOT 연산자가 반환하는 값의 대상을 확인합니다.',
    statement: 'CONNECT_BY_ROOT는 현재 행의 부모를 반환한다.',
    answer: 'X',
    hints: {
      directional: ['ROOT라는 단어의 의미를 주목하세요'],
      misconception: ['CONNECT_BY_ROOT는 바로 위 부모가 아니라, 계층의 최상위 루트 행의 값을 반환합니다'],
    },
    learningPoint: 'CONNECT_BY_ROOT는 현재 행이 속한 계층의 최상위 루트 행의 컬럼 값을 반환한다',
    tags: ['CONNECT_BY_ROOT', 'CONNECT BY'],
    explanation: 'CONNECT_BY_ROOT는 바로 위 부모(PRIOR)가 아니라, 해당 계층 트리의 최상위 루트 행의 컬럼 값을 반환합니다. 예를 들어 3단계 깊이의 직원이라면, 직속 상사가 아닌 최상위 임원의 정보를 가져옵니다.',
    relatedConceptTags: ['sql-advanced'],
  } as OxProblem,
  {
    id: 'avo06',
    category: 'sql-advanced',
    difficulty: 'intermediate',
    type: 'ox',
    availableModes: ['write', 'fill'] as const,
    title: '재귀 CTE의 DBMS 지원 범위',
    question: '재귀 CTE가 오라클에서만 사용 가능한지 확인합니다.',
    statement: '재귀 CTE는 오라클에서만 사용 가능하다.',
    answer: 'X',
    hints: {
      directional: ['재귀 CTE가 ISO SQL 표준인지 생각해보세요'],
      misconception: ['재귀 CTE(WITH RECURSIVE)는 SQL 표준이며, PostgreSQL, MySQL 8.0+, SQL Server 등 대부분의 RDBMS에서 지원합니다'],
    },
    learningPoint: '재귀 CTE(WITH RECURSIVE)는 SQL:1999 표준이며 대부분의 주요 RDBMS에서 지원한다',
    tags: ['WITH RECURSIVE', 'CTE', '표준SQL'],
    explanation: '재귀 CTE는 SQL:1999 표준에 포함된 기능으로, 오라클뿐 아니라 PostgreSQL, MySQL 8.0+, SQL Server, MariaDB 등 대부분의 주요 RDBMS에서 지원합니다. 오라클 전용인 것은 CONNECT BY입니다.',
    relatedConceptTags: ['sql-advanced'],
  } as OxProblem,
  {
    id: 'avo07',
    category: 'sql-advanced',
    difficulty: 'advanced',
    type: 'ox',
    availableModes: ['write', 'fill'] as const,
    title: 'SYS_CONNECT_BY_PATH의 연결 대상 데이터 타입',
    question: 'SYS_CONNECT_BY_PATH 함수가 숫자만 연결할 수 있는지 확인합니다.',
    statement: 'SYS_CONNECT_BY_PATH는 숫자만 연결할 수 있다.',
    answer: 'X',
    hints: {
      directional: ['SYS_CONNECT_BY_PATH가 경로를 문자열로 반환한다는 점을 생각해보세요'],
      misconception: ['SYS_CONNECT_BY_PATH는 컬럼 값을 문자열로 변환하여 연결하므로, 숫자뿐 아니라 문자열 컬럼도 사용 가능합니다'],
    },
    learningPoint: 'SYS_CONNECT_BY_PATH는 숫자, 문자열 등 다양한 데이터 타입의 컬럼 값을 문자열로 연결할 수 있다',
    tags: ['SYS_CONNECT_BY_PATH', 'CONNECT BY'],
    explanation: 'SYS_CONNECT_BY_PATH(컬럼, 구분자) 함수는 루트부터 현재 행까지의 경로를 구분자로 연결한 문자열을 반환합니다. 컬럼이 숫자 타입이면 자동으로 문자열로 변환되며, 문자열 컬럼도 당연히 사용할 수 있습니다.',
    relatedConceptTags: ['sql-advanced'],
  } as OxProblem,
  {
    id: 'avo08',
    category: 'sql-advanced',
    difficulty: 'advanced',
    type: 'ox',
    availableModes: ['write', 'fill'] as const,
    title: 'CONNECT BY PRIOR 방향 전환의 효과',
    question: 'CONNECT BY PRIOR의 컬럼 위치를 바꾸면 탐색 방향이 반대가 되는지 확인합니다.',
    statement: 'CONNECT BY PRIOR 방향을 바꾸면 탐색 방향이 반대가 된다.',
    answer: 'O',
    hints: {
      directional: ['PRIOR가 PK 앞에 오는 경우와 FK 앞에 오는 경우의 차이를 생각해보세요'],
      misconception: ['PRIOR PK = FK는 하향식(부모→자식), PRIOR FK = PK는 상향식(자식→부모) 탐색입니다'],
    },
    learningPoint: 'PRIOR의 위치에 따라 하향식(PRIOR PK = FK)과 상향식(PRIOR FK = PK) 탐색이 결정된다',
    tags: ['CONNECT BY', 'PRIOR', '탐색방향'],
    explanation: 'CONNECT BY PRIOR emp_id = manager_id는 부모→자식 하향식 탐색이고, CONNECT BY PRIOR manager_id = emp_id는 자식→부모 상향식 탐색입니다. PRIOR가 붙은 컬럼이 현재 행에서 참조하는 값이 되어 다음 행을 찾습니다.',
    relatedConceptTags: ['sql-advanced'],
  } as OxProblem,

  // ── avp01 ~ avp02: Plan 문제 (계층형 쿼리 + CTE 실행계획) ────────
  {
    id: 'avp01',
    category: 'sql-advanced',
    difficulty: 'intermediate',
    type: 'plan',
    availableModes: ['write', 'fill'] as const,
    title: '조직도 계층 쿼리 실행계획 분석',
    question: '아래 실행계획은 employees 테이블에서 조직도를 CONNECT BY로 조회한 결과입니다. 가장 비용이 큰 병목 단계는?',
    learningPoint: 'CONNECT BY 계층 쿼리에서 TABLE ACCESS FULL이 발생하면 대량 데이터 시 성능 병목이 된다',
    tags: ['실행계획', 'CONNECT BY', '조직도'],
    explanation: 'CONNECT BY 계층 쿼리는 내부적으로 CONNECT BY 연산을 수행하며, 하위 테이블 접근이 TABLE ACCESS FULL이면 매 단계마다 전체 테이블을 스캔합니다. manager_id에 인덱스를 생성하면 INDEX RANGE SCAN으로 개선할 수 있습니다.',
    relatedConceptTags: ['sql-advanced'],
    planText: `--------------------------------------------------------------------
| Id  | Operation                     | Name      | Rows | Cost (%CPU)|
---------------------------------------------------------------------
|   0 | SELECT STATEMENT              |           |  500 |   320  (2)|
|*  1 |  CONNECT BY WITH FILTERING    |           |      |           |
|   2 |   TABLE ACCESS FULL           | EMPLOYEES |  500 |   150  (2)|
|*  3 |   HASH JOIN                   |           |  500 |   170  (3)|
|   4 |    CONNECT BY PUMP            |           |      |           |
|   5 |    TABLE ACCESS FULL          | EMPLOYEES |  500 |   150  (2)|
---------------------------------------------------------------------`,
    choices: ['SELECT STATEMENT', 'CONNECT BY WITH FILTERING', 'TABLE ACCESS FULL (Id=2)', 'HASH JOIN'],
    choiceExplanations: [
      { value: 'SELECT STATEMENT', explanation: '최상위 실행 노드로, 하위 단계의 비용을 합산한 것입니다. 실제 병목 원인이 아닙니다' },
      { value: 'CONNECT BY WITH FILTERING', explanation: '계층 탐색을 제어하는 논리적 연산으로, 실제 I/O를 발생시키는 단계가 아닙니다' },
      { value: 'TABLE ACCESS FULL (Id=2)', explanation: '500행의 EMPLOYEES 테이블을 전체 스캔합니다. CONNECT BY 내부에서 반복 호출되어 총 비용이 커집니다' },
      { value: 'HASH JOIN', explanation: 'HASH JOIN은 CONNECT BY 펌프와 테이블을 조인하는 단계이며, 전체 스캔이 더 큰 병목입니다' },
    ],
    correctAnswer: 'TABLE ACCESS FULL (Id=2)',
    hints: {
      directional: ['CONNECT BY 내부에서 테이블을 어떻게 접근하는지 주목하세요'],
      constraint: ['FULL이라는 키워드는 전체 테이블 스캔을 의미합니다'],
      misconception: ['CONNECT BY WITH FILTERING은 논리적 제어 단계이지 실제 I/O를 발생시키는 것은 테이블 접근 단계입니다'],
    },
  } as PlanProblem,
  {
    id: 'avp02',
    category: 'sql-advanced',
    difficulty: 'advanced',
    type: 'plan',
    availableModes: ['write', 'fill'] as const,
    title: '재귀 CTE 실행계획에서 성능 개선점 파악',
    question: '아래 실행계획은 댓글 테이블에서 재귀 CTE로 대댓글 트리를 조회한 결과입니다. 성능 개선을 위해 가장 먼저 조치해야 할 사항은?',
    learningPoint: '재귀 CTE에서 재귀 멤버의 조인 조건 컬럼에 인덱스가 없으면 매 반복마다 전체 스캔이 발생한다',
    tags: ['실행계획', 'WITH RECURSIVE', 'CTE', '대댓글'],
    explanation: '재귀 CTE는 앵커 멤버 실행 후 재귀 멤버를 반복 실행합니다. 재귀 멤버에서 COMMENTS 테이블을 TABLE ACCESS FULL로 접근하면, 계층 깊이가 깊을수록 전체 스캔이 반복됩니다. parent_comment_id에 인덱스를 생성하면 INDEX RANGE SCAN으로 개선됩니다.',
    relatedConceptTags: ['sql-advanced'],
    planText: `------------------------------------------------------------------------
| Id  | Operation                          | Name     | Rows | Cost (%CPU)|
-------------------------------------------------------------------------
|   0 | SELECT STATEMENT                   |          | 2000 |   890  (3)|
|   1 |  TEMP TABLE TRANSFORMATION         |          |      |           |
|   2 |   LOAD AS SELECT                   |          |      |           |
|*  3 |    TABLE ACCESS FULL               | COMMENTS |    1 |    45  (2)|
|   4 |   UNION ALL (RECURSIVE WITH)       |          |      |           |
|   5 |    TABLE ACCESS FULL               | SYS_TEMP |  200 |    12  (0)|
|*  6 |    HASH JOIN                       |          | 1800 |   833  (3)|
|   7 |     RECURSIVE WITH PUMP            |          |      |           |
|   8 |     TABLE ACCESS FULL              | COMMENTS | 2000 |   420  (2)|
-------------------------------------------------------------------------`,
    choices: [
      'TEMP TABLE TRANSFORMATION을 제거한다',
      'parent_comment_id 컬럼에 인덱스를 생성한다',
      'UNION ALL을 UNION으로 변경한다',
      'RECURSIVE WITH PUMP를 튜닝한다',
    ],
    choiceExplanations: [
      { value: 'TEMP TABLE TRANSFORMATION을 제거한다', explanation: '임시 테이블 변환은 오라클이 재귀 CTE를 처리하는 내부 방식이며, 사용자가 직접 제거할 수 없습니다' },
      { value: 'parent_comment_id 컬럼에 인덱스를 생성한다', explanation: '재귀 멤버에서 COMMENTS를 TABLE ACCESS FULL(Cost 420)로 접근하는 것이 병목입니다. 조인 조건인 parent_comment_id에 인덱스를 생성하면 INDEX RANGE SCAN으로 개선됩니다' },
      { value: 'UNION ALL을 UNION으로 변경한다', explanation: 'UNION은 중복 제거 비용이 추가되어 오히려 성능이 저하됩니다. 재귀 CTE에서는 UNION ALL이 표준입니다' },
      { value: 'RECURSIVE WITH PUMP를 튜닝한다', explanation: 'RECURSIVE WITH PUMP는 오라클 내부 연산으로, 사용자가 직접 튜닝할 수 없습니다' },
    ],
    correctAnswer: 'parent_comment_id 컬럼에 인덱스를 생성한다',
    hints: {
      directional: ['재귀 멤버에서 반복적으로 접근하는 테이블의 접근 방식을 확인하세요'],
      constraint: ['TABLE ACCESS FULL이 재귀적으로 반복되면 비용이 기하급수적으로 증가합니다'],
      misconception: ['TEMP TABLE TRANSFORMATION이나 RECURSIVE WITH PUMP는 오라클 내부 연산이며 사용자가 직접 제어할 수 없습니다'],
    },
  } as PlanProblem,
];
