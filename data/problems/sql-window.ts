import type { WriteProblem, FillProblem, OxProblem, PlanProblem } from '@/types/problem';

export const SQL_WINDOW_PROBLEMS = [
  {
    id: 'sww01',
    category: 'sql-window',
    difficulty: 'intermediate',
    type: 'write',
    availableModes: ['write'] as const,
    title: '부서별 급여 순위 (ROW_NUMBER)',
    question: 'employees 테이블에서 부서별(department_id)로 급여(salary) 내림차순 순위를 매기세요. 결과 컬럼은 name, department_id, salary, rn(순위)입니다.',
    learningPoint: 'ROW_NUMBER() + PARTITION BY + ORDER BY 조합',
    tags: ['ROW_NUMBER', 'PARTITION BY', 'ORDER BY'],
    explanation: 'ROW_NUMBER()는 PARTITION BY로 나눈 각 그룹 내에서 ORDER BY 기준으로 순차적인 번호를 부여합니다. 동일 값이어도 고유한 번호가 매겨집니다.',
    relatedConceptTags: ['window-function'],
    schema: 'employees (id, name, department_id, salary)',
    sampleData: {
      columns: ['name', 'department_id', 'salary', 'rn'],
      rows: [['김철수', '10', '7000', '1'], ['박민수', '10', '5000', '2'], ['이영희', '20', '6000', '1']],
    },
    acceptableAnswers: [
      'SELECT name, department_id, salary, ROW_NUMBER() OVER (PARTITION BY department_id ORDER BY salary DESC) AS rn FROM employees',
      'SELECT name, department_id, salary, ROW_NUMBER() OVER(PARTITION BY department_id ORDER BY salary DESC) rn FROM employees',
    ],
    hints: {
      directional: ['ROW_NUMBER() OVER(...) 구문을 사용하세요'],
      constraint: ['PARTITION BY로 부서별 그룹을 나누세요'],
      misconception: ['ORDER BY salary DESC로 급여 내림차순 정렬. ASC는 오름차순입니다'],
    },
  } as WriteProblem,
  {
    id: 'swf01',
    category: 'sql-window',
    difficulty: 'intermediate',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: 'RANK 함수 빈칸 채우기',
    question: '아래 SQL의 빈칸을 채워 전체 직원의 급여 순위를 매기세요. 동일 급여는 같은 순위를 부여합니다.',
    learningPoint: 'RANK()는 동일 값에 같은 순위를 부여하고 다음 순위를 건너뛴다',
    tags: ['RANK', 'OVER'],
    explanation: 'RANK()는 동일 값에 같은 순위를 부여합니다. 예: 1등이 2명이면 다음은 3등. DENSE_RANK()와 달리 순위를 건너뜁니다.',
    relatedConceptTags: ['window-function'],
    sqlTemplate: 'SELECT name, salary, ___() ___(ORDER BY salary DESC) AS rnk FROM employees;',
    blanks: 2,
    options: ['RANK', 'ROW_NUMBER', 'DENSE_RANK', 'OVER', 'PARTITION BY', 'GROUP BY'],
    optionExplanations: [
      { value: 'RANK', explanation: '동일 값에 같은 순위를 부여하고, 다음 순위를 건너뜁니다 (1,1,3)' },
      { value: 'ROW_NUMBER', explanation: '항상 고유한 순번을 부여합니다. 동일 값이어도 다른 번호가 매겨집니다' },
      { value: 'DENSE_RANK', explanation: '동일 값에 같은 순위를 부여하지만, 다음 순위를 건너뛰지 않습니다 (1,1,2)' },
      { value: 'OVER', explanation: '윈도우 함수의 적용 범위를 정의하는 키워드입니다' },
      { value: 'PARTITION BY', explanation: '윈도우를 그룹으로 나누는 키워드입니다. OVER 내부에서 사용됩니다' },
      { value: 'GROUP BY', explanation: '집계 함수와 함께 그룹을 만듭니다. 윈도우 함수와는 다른 개념입니다' },
    ],
    correctAnswers: ['RANK', 'OVER'],
    hints: {
      directional: ['동일 값에 같은 순위를 부여하는 함수를 찾으세요'],
      constraint: ['윈도우 함수 뒤에 오는 키워드가 필요합니다'],
      misconception: ['ROW_NUMBER는 동일 값이어도 고유 번호를 부여합니다. RANK와 다릅니다'],
    },
  } as FillProblem,
  {
    id: 'swo01',
    category: 'sql-window',
    difficulty: 'intermediate',
    type: 'ox',
    availableModes: ['write', 'fill'] as const,
    title: 'ROW_NUMBER vs RANK',
    question: 'ROW_NUMBER와 RANK의 차이를 정확히 이해하고 있는지 확인합니다.',
    learningPoint: 'ROW_NUMBER는 항상 고유, RANK는 동일 값에 같은 순위',
    tags: ['ROW_NUMBER', 'RANK'],
    explanation: 'ROW_NUMBER()는 동일 값이어도 항상 고유한 순번을 부여합니다(1,2,3...). RANK()는 동일 값에 같은 순위를 부여하고 다음 순위를 건너뜁니다(1,1,3...).',
    relatedConceptTags: ['window-function'],
    statement: '급여가 동일한 두 직원이 있을 때, ROW_NUMBER()는 둘에게 같은 번호를 부여한다.',
    answer: 'X',
    hints: {
      directional: ['ROW_NUMBER()가 부여하는 번호의 특성을 생각해보세요'],
      misconception: ['ROW_NUMBER()는 항상 유일한 순번을 부여합니다. 같은 번호를 주는 것은 RANK()입니다'],
    },
  } as OxProblem,
  // ── swf02 ~ swf16 ──────────────────────────────────────────────
  {
    id: 'swf02',
    category: 'sql-window',
    difficulty: 'basic',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '게임 점수 전체 순위 매기기',
    question: '아래 SQL의 빈칸을 채워 scores 테이블에서 점수(score) 내림차순으로 전체 순위를 매기세요. 동일 점수여도 고유한 순번을 부여합니다.',
    learningPoint: 'ROW_NUMBER()는 동일 값이어도 항상 고유한 순번을 부여한다',
    tags: ['ROW_NUMBER', 'OVER', 'ORDER BY'],
    explanation: 'ROW_NUMBER() OVER (ORDER BY score DESC)는 점수 내림차순으로 각 행에 고유한 번호를 부여합니다. 동점자가 있어도 각각 다른 번호가 매겨집니다.',
    relatedConceptTags: ['window-function'],
    sqlTemplate: 'SELECT user_id, score, ___()\nOVER (___ score DESC) AS ranking\nFROM scores;',
    blanks: 2,
    options: ['ROW_NUMBER', 'RANK', 'DENSE_RANK', 'ORDER BY', 'PARTITION BY', 'GROUP BY', 'SORT BY'],
    optionExplanations: [
      { value: 'ROW_NUMBER', explanation: '항상 고유한 순번을 부여합니다. 동일 값이어도 서로 다른 번호가 매겨집니다' },
      { value: 'RANK', explanation: '동일 값에 같은 순위를 부여하고 다음 순위를 건너뜁니다 (1,1,3)' },
      { value: 'DENSE_RANK', explanation: '동일 값에 같은 순위를 부여하지만 다음 순위를 건너뛰지 않습니다 (1,1,2)' },
      { value: 'ORDER BY', explanation: 'OVER 절 안에서 정렬 기준을 지정합니다' },
      { value: 'PARTITION BY', explanation: '윈도우를 그룹으로 나눌 때 사용합니다. 여기서는 그룹 구분 없이 전체를 대상으로 합니다' },
      { value: 'GROUP BY', explanation: '집계 함수와 함께 그룹핑에 사용합니다. OVER 절 내에서는 사용할 수 없습니다' },
      { value: 'SORT BY', explanation: 'SQL 표준 키워드가 아닙니다. 일부 Hive SQL에서만 사용됩니다' },
    ],
    correctAnswers: ['ROW_NUMBER', 'ORDER BY'],
    hints: {
      directional: ['동일 점수여도 고유 번호를 부여하는 함수를 고르세요'],
      constraint: ['OVER 절 안에서 정렬 방향을 지정하는 키워드가 필요합니다'],
      misconception: ['RANK는 동점 시 같은 순위를 부여합니다. 고유 번호는 ROW_NUMBER입니다'],
    },
  } as FillProblem,
  {
    id: 'swf03',
    category: 'sql-window',
    difficulty: 'basic',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '카테고리별 상품 가격 순위 (DENSE_RANK)',
    question: '아래 SQL의 빈칸을 채워 products 테이블에서 카테고리(category)별로 가격(price) 내림차순 순위를 매기세요. 동일 가격은 같은 순위를 주되 순위를 건너뛰지 않습니다.',
    learningPoint: 'DENSE_RANK()는 동일 값에 같은 순위를 부여하되 순위 번호를 건너뛰지 않는다',
    tags: ['DENSE_RANK', 'PARTITION BY', 'ORDER BY'],
    explanation: 'DENSE_RANK() OVER (PARTITION BY category ORDER BY price DESC)는 카테고리별로 나눠 가격 내림차순으로 순위를 매깁니다. 동점 시 1,1,2,3... 형태로 순위가 이어집니다.',
    relatedConceptTags: ['window-function'],
    sqlTemplate: 'SELECT product_id, category, price,\n  ___()\n  OVER (PARTITION BY category ___ price DESC) AS price_rank\nFROM products;',
    blanks: 2,
    options: ['DENSE_RANK', 'RANK', 'ROW_NUMBER', 'ORDER BY', 'PARTITION BY', 'HAVING', 'LIMIT'],
    optionExplanations: [
      { value: 'DENSE_RANK', explanation: '동일 값에 같은 순위를 부여하고 순위를 건너뛰지 않습니다 (1,1,2,3)' },
      { value: 'RANK', explanation: '동일 값에 같은 순위를 부여하지만 다음 순위를 건너뜁니다 (1,1,3)' },
      { value: 'ROW_NUMBER', explanation: '동일 값이어도 항상 고유한 순번을 부여합니다' },
      { value: 'ORDER BY', explanation: 'OVER 절 안에서 순위 기준 정렬을 지정합니다' },
      { value: 'PARTITION BY', explanation: '윈도우를 그룹으로 나눕니다. 이미 앞에 있으므로 빈칸은 정렬 키워드입니다' },
      { value: 'HAVING', explanation: '집계 결과를 필터링하는 절입니다. OVER 안에서 사용할 수 없습니다' },
      { value: 'LIMIT', explanation: '결과 행 수를 제한하는 절입니다. OVER 안에서 사용할 수 없습니다' },
    ],
    correctAnswers: ['DENSE_RANK', 'ORDER BY'],
    hints: {
      directional: ['순위를 건너뛰지 않는 순위 함수를 고르세요'],
      constraint: ['PARTITION BY 뒤에 올 정렬 키워드를 채우세요'],
      misconception: ['RANK는 동점 시 다음 순위를 건너뜁니다. 건너뛰지 않으려면 DENSE_RANK를 사용하세요'],
    },
  } as FillProblem,
  {
    id: 'swf04',
    category: 'sql-window',
    difficulty: 'basic',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '광고 이벤트 이전 행 값 가져오기 (LAG)',
    question: '아래 SQL의 빈칸을 채워 ad_logs 테이블에서 각 광고(ad_id)별로 이전 이벤트의 created_at을 가져오세요.',
    learningPoint: 'LAG()는 현재 행 기준 이전 행의 값을 반환한다',
    tags: ['LAG', 'PARTITION BY', 'ORDER BY'],
    explanation: 'LAG(created_at) OVER (PARTITION BY ad_id ORDER BY created_at)는 같은 광고 내에서 바로 이전 이벤트의 시간을 가져옵니다. 첫 번째 행은 이전 값이 없으므로 NULL을 반환합니다.',
    relatedConceptTags: ['window-function'],
    sqlTemplate: 'SELECT ad_id, user_id, created_at,\n  ___(created_at)\n  OVER (PARTITION BY ad_id ORDER BY created_at) AS prev_event_time\nFROM ad_logs;',
    blanks: 1,
    options: ['LAG', 'LEAD', 'FIRST_VALUE', 'LAST_VALUE', 'LAG(1)', 'PREV'],
    optionExplanations: [
      { value: 'LAG', explanation: '현재 행 기준 이전 행의 값을 반환합니다. 기본 오프셋은 1입니다' },
      { value: 'LEAD', explanation: '현재 행 기준 다음 행의 값을 반환합니다. 이전 값이 아닌 이후 값입니다' },
      { value: 'FIRST_VALUE', explanation: '윈도우 프레임에서 첫 번째 행의 값을 반환합니다' },
      { value: 'LAST_VALUE', explanation: '윈도우 프레임에서 마지막 행의 값을 반환합니다' },
      { value: 'LAG(1)', explanation: 'LAG 함수는 컬럼명을 첫 인자로 받습니다. LAG(1)은 올바른 문법이 아닙니다' },
      { value: 'PREV', explanation: 'SQL 표준에 PREV 함수는 존재하지 않습니다' },
    ],
    correctAnswers: ['LAG'],
    hints: {
      directional: ['이전 행의 값을 가져오는 윈도우 함수를 고르세요'],
      misconception: ['LEAD는 다음 행의 값을 가져옵니다. 이전 행은 LAG입니다'],
    },
  } as FillProblem,
  {
    id: 'swf05',
    category: 'sql-window',
    difficulty: 'basic',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '구독 플랜별 다음 시작일 미리 보기 (LEAD)',
    question: '아래 SQL의 빈칸을 채워 subscriptions 테이블에서 사용자(user_id)별 다음 구독 시작일(start_date)을 조회하세요.',
    learningPoint: 'LEAD()는 현재 행 기준 다음 행의 값을 반환한다',
    tags: ['LEAD', 'PARTITION BY', 'ORDER BY'],
    explanation: 'LEAD(start_date) OVER (PARTITION BY user_id ORDER BY start_date)는 같은 사용자의 다음 구독 시작일을 가져옵니다. 마지막 행은 다음 값이 없어 NULL을 반환합니다.',
    relatedConceptTags: ['window-function'],
    sqlTemplate: 'SELECT user_id, plan_id, start_date,\n  ___(start_date)\n  OVER (___ user_id ORDER BY start_date) AS next_start_date\nFROM subscriptions;',
    blanks: 2,
    options: ['LEAD', 'LAG', 'NEXT_VALUE', 'PARTITION BY', 'GROUP BY', 'ORDER BY', 'CLUSTER BY'],
    optionExplanations: [
      { value: 'LEAD', explanation: '현재 행 기준 다음 행의 값을 반환합니다' },
      { value: 'LAG', explanation: '현재 행 기준 이전 행의 값을 반환합니다. 이전 구독일을 조회할 때 사용합니다' },
      { value: 'NEXT_VALUE', explanation: 'SQL 표준에 NEXT_VALUE 함수는 존재하지 않습니다' },
      { value: 'PARTITION BY', explanation: 'OVER 절 안에서 윈도우를 그룹으로 나눕니다' },
      { value: 'GROUP BY', explanation: '집계 함수와 함께 그룹핑에 사용됩니다. OVER 절 내에서 사용할 수 없습니다' },
      { value: 'ORDER BY', explanation: '결과 정렬 또는 OVER 절 내 정렬 기준입니다. 여기선 PARTITION BY 위치에 맞지 않습니다' },
      { value: 'CLUSTER BY', explanation: 'Hive SQL 전용 키워드입니다. 표준 SQL OVER 절에서 사용할 수 없습니다' },
    ],
    correctAnswers: ['LEAD', 'PARTITION BY'],
    hints: {
      directional: ['다음 행의 값을 가져오는 함수와 그룹을 나누는 키워드를 찾으세요'],
      constraint: ['OVER 절에서 user_id 앞에 올 그룹 분리 키워드가 필요합니다'],
      misconception: ['LAG는 이전 행, LEAD는 다음 행의 값을 반환합니다'],
    },
  } as FillProblem,
  {
    id: 'swf06',
    category: 'sql-window',
    difficulty: 'intermediate',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '배달 음식점별 누적 평점 합계',
    question: '아래 SQL의 빈칸을 채워 deliveries 테이블에서 음식점(restaurant_id)별로 delivered_at 순서 기준 누적 평점 합계를 구하세요.',
    learningPoint: 'SUM() OVER (PARTITION BY ... ORDER BY ...)로 누적 합계를 구할 수 있다',
    tags: ['SUM', 'OVER', 'PARTITION BY', 'ORDER BY'],
    explanation: 'SUM(rating) OVER (PARTITION BY restaurant_id ORDER BY delivered_at)는 음식점별로 delivered_at 순서에 따른 누적 평점 합계를 계산합니다.',
    relatedConceptTags: ['window-function'],
    sqlTemplate: 'SELECT order_id, restaurant_id, rating, delivered_at,\n  ___(rating)\n  OVER (PARTITION BY restaurant_id ___ delivered_at) AS cumulative_rating\nFROM deliveries;',
    blanks: 2,
    options: ['SUM', 'AVG', 'COUNT', 'ORDER BY', 'PARTITION BY', 'ROWS BETWEEN', 'MAX'],
    optionExplanations: [
      { value: 'SUM', explanation: '누적 합계를 구하는 집계 함수입니다. OVER와 함께 쓰면 윈도우 함수로 동작합니다' },
      { value: 'AVG', explanation: '평균을 구합니다. 누적 합계가 아닌 누적 평균을 구할 때 사용합니다' },
      { value: 'COUNT', explanation: '행 수를 셉니다. 평점 합계가 아닌 건수를 구할 때 사용합니다' },
      { value: 'ORDER BY', explanation: 'OVER 절 안에서 누적 기준 정렬을 지정합니다. ORDER BY가 있으면 기본 프레임이 누적 합계로 동작합니다' },
      { value: 'PARTITION BY', explanation: '윈도우를 그룹으로 나눕니다. 이미 앞에 있으므로 이 위치는 정렬 키워드입니다' },
      { value: 'ROWS BETWEEN', explanation: '윈도우 프레임 범위를 명시적으로 지정할 때 사용합니다. ORDER BY 뒤에 옵니다' },
      { value: 'MAX', explanation: '최댓값을 구합니다. 누적 합계와는 다릅니다' },
    ],
    correctAnswers: ['SUM', 'ORDER BY'],
    hints: {
      directional: ['누적 합계를 구하는 집계 함수와 OVER 안 정렬 키워드를 찾으세요'],
      constraint: ['ORDER BY가 OVER 절에 있으면 기본 프레임이 누적 합계로 동작합니다'],
      misconception: ['AVG는 평균이지 합계가 아닙니다. 누적 합계는 SUM을 사용하세요'],
    },
  } as FillProblem,
  {
    id: 'swf07',
    category: 'sql-window',
    difficulty: 'intermediate',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '스트리밍 콘텐츠별 평균 시청 시간 비교',
    question: '아래 SQL의 빈칸을 채워 watch_logs 테이블에서 콘텐츠(content_id)별 평균 시청 시간(duration_sec)을 각 행과 함께 표시하세요.',
    learningPoint: 'AVG() OVER (PARTITION BY ...)로 그룹 평균을 각 행에 함께 표시할 수 있다',
    tags: ['AVG', 'OVER', 'PARTITION BY'],
    explanation: 'AVG(duration_sec) OVER (PARTITION BY content_id)는 GROUP BY 없이도 콘텐츠별 평균 시청 시간을 각 행에 함께 표시합니다. 원본 행을 유지하면서 집계값을 볼 수 있습니다.',
    relatedConceptTags: ['window-function'],
    sqlTemplate: 'SELECT user_id, content_id, duration_sec,\n  ___(duration_sec)\n  OVER (___ content_id) AS avg_duration\nFROM watch_logs;',
    blanks: 2,
    options: ['AVG', 'SUM', 'MAX', 'PARTITION BY', 'ORDER BY', 'GROUP BY', 'DISTRIBUTE BY'],
    optionExplanations: [
      { value: 'AVG', explanation: '평균을 구하는 집계 함수입니다. OVER와 함께 쓰면 각 행을 유지하면서 그룹 평균을 반환합니다' },
      { value: 'SUM', explanation: '합계를 구합니다. 평균이 아닌 합계를 구할 때 사용합니다' },
      { value: 'MAX', explanation: '최댓값을 구합니다. 평균과는 다릅니다' },
      { value: 'PARTITION BY', explanation: 'OVER 절 안에서 그룹을 나눠 각 그룹 내에서 집계합니다' },
      { value: 'ORDER BY', explanation: '정렬 기준을 지정합니다. 단순 그룹 평균에는 ORDER BY가 필요 없습니다' },
      { value: 'GROUP BY', explanation: '집계 함수와 함께 쓰는 그룹핑 절입니다. OVER 절 내에서는 사용할 수 없습니다' },
      { value: 'DISTRIBUTE BY', explanation: 'Hive SQL 전용 키워드입니다. 표준 SQL OVER 절에서 사용할 수 없습니다' },
    ],
    correctAnswers: ['AVG', 'PARTITION BY'],
    hints: {
      directional: ['평균 집계 함수와 그룹을 나누는 키워드를 고르세요'],
      constraint: ['ORDER BY 없이 PARTITION BY만으로 그룹 평균을 구할 수 있습니다'],
      misconception: ['GROUP BY를 쓰면 원본 행이 사라집니다. 행을 유지하려면 OVER(PARTITION BY)를 사용하세요'],
    },
  } as FillProblem,
  {
    id: 'swf08',
    category: 'sql-window',
    difficulty: 'intermediate',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '주문 금액 내 첫 번째 주문 금액 확인 (FIRST_VALUE)',
    question: '아래 SQL의 빈칸을 채워 orders 테이블에서 사용자(user_id)별로 가장 첫 번째 주문 금액(amount)을 각 행과 함께 표시하세요.',
    learningPoint: 'FIRST_VALUE()는 윈도우 프레임의 첫 번째 행 값을 반환한다',
    tags: ['FIRST_VALUE', 'PARTITION BY', 'ORDER BY'],
    explanation: 'FIRST_VALUE(amount) OVER (PARTITION BY user_id ORDER BY created_at)는 사용자별로 created_at 기준 첫 번째 주문 금액을 각 행에 함께 반환합니다.',
    relatedConceptTags: ['window-function'],
    sqlTemplate: 'SELECT order_id, user_id, amount, created_at,\n  ___(amount)\n  OVER (PARTITION BY user_id ORDER BY created_at) AS first_order_amount\nFROM orders;',
    blanks: 1,
    options: ['FIRST_VALUE', 'LAST_VALUE', 'LAG', 'MIN', 'NTH_VALUE', 'LEAD', 'ROW_NUMBER'],
    optionExplanations: [
      { value: 'FIRST_VALUE', explanation: '윈도우 프레임에서 첫 번째 행의 값을 반환합니다' },
      { value: 'LAST_VALUE', explanation: '윈도우 프레임에서 마지막 행의 값을 반환합니다. 기본 프레임 범위에 주의해야 합니다' },
      { value: 'LAG', explanation: '현재 행 기준 이전 행의 값을 반환합니다. 항상 첫 번째 행이 아닌 바로 이전 행입니다' },
      { value: 'MIN', explanation: '최솟값을 반환합니다. 날짜 기준 첫 번째가 금액 최솟값과 다를 수 있습니다' },
      { value: 'NTH_VALUE', explanation: 'N번째 행의 값을 반환합니다. 첫 번째는 NTH_VALUE(col, 1)로도 가능하지만 FIRST_VALUE가 더 명확합니다' },
      { value: 'LEAD', explanation: '다음 행의 값을 반환합니다. 첫 번째 행 값이 아닙니다' },
      { value: 'ROW_NUMBER', explanation: '순번을 반환합니다. 값을 가져오는 함수가 아닙니다' },
    ],
    correctAnswers: ['FIRST_VALUE'],
    hints: {
      directional: ['파티션 내 첫 번째 행의 값을 반환하는 함수를 고르세요'],
      misconception: ['MIN은 최솟값입니다. 날짜 기준 첫 번째 금액과 다를 수 있으므로 FIRST_VALUE를 사용하세요'],
    },
  } as FillProblem,
  {
    id: 'swf09',
    category: 'sql-window',
    difficulty: 'intermediate',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '게임 유저별 최근 플레이 점수 확인 (LAST_VALUE)',
    question: '아래 SQL의 빈칸을 채워 scores 테이블에서 유저(user_id)별로 가장 최근 플레이 점수(score)를 각 행과 함께 표시하세요. 윈도우 프레임은 전체 파티션으로 설정하세요.',
    learningPoint: 'LAST_VALUE()는 ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING으로 전체 파티션의 마지막 값을 정확히 가져올 수 있다',
    tags: ['LAST_VALUE', 'ROWS BETWEEN', 'PARTITION BY'],
    explanation: 'LAST_VALUE는 기본 프레임(RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)이 현재 행까지만이므로, 마지막 값을 제대로 가져오려면 ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING을 명시해야 합니다.',
    relatedConceptTags: ['window-function'],
    sqlTemplate: 'SELECT user_id, score, played_at,\n  LAST_VALUE(score)\n  OVER (PARTITION BY user_id ORDER BY played_at\n        ___ UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING) AS latest_score\nFROM scores;',
    blanks: 1,
    options: ['ROWS BETWEEN', 'RANGE BETWEEN', 'FRAME BY', 'WINDOW BETWEEN', 'BETWEEN ROWS', 'ROWS'],
    optionExplanations: [
      { value: 'ROWS BETWEEN', explanation: '물리적 행 기준으로 윈도우 프레임 범위를 지정합니다. LAST_VALUE와 함께 자주 사용됩니다' },
      { value: 'RANGE BETWEEN', explanation: '논리적 값 범위 기준으로 프레임을 지정합니다. ROWS BETWEEN과 다르게 동작할 수 있습니다' },
      { value: 'FRAME BY', explanation: 'SQL 표준에 존재하지 않는 키워드입니다' },
      { value: 'WINDOW BETWEEN', explanation: 'SQL 표준에 존재하지 않는 키워드입니다' },
      { value: 'BETWEEN ROWS', explanation: '올바른 SQL 문법이 아닙니다. ROWS BETWEEN이 올바른 순서입니다' },
      { value: 'ROWS', explanation: 'ROWS 단독으로는 프레임 범위를 지정할 수 없습니다. ROWS BETWEEN이 올바른 형태입니다' },
    ],
    correctAnswers: ['ROWS BETWEEN'],
    hints: {
      directional: ['물리적 행 기준으로 프레임 범위를 지정하는 키워드를 찾으세요'],
      constraint: ['UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING 앞에 오는 두 단어 키워드입니다'],
      misconception: ['LAST_VALUE는 기본 프레임이 현재 행까지라 마지막 값이 제대로 안 나올 수 있습니다. 프레임 명시가 중요합니다'],
    },
  } as FillProblem,
  {
    id: 'swf10',
    category: 'sql-window',
    difficulty: 'intermediate',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '광고 캠페인별 이전 이벤트와 현재 이벤트 시간 차 계산',
    question: '아래 SQL의 빈칸을 채워 ad_logs 테이블에서 광고(ad_id)별로 이전 이벤트 대비 현재 이벤트의 created_at을 LAG로 가져오고, 2행 이전 값도 가져오세요.',
    learningPoint: 'LAG(col, N)의 두 번째 인자로 몇 행 이전 값을 가져올지 지정할 수 있다',
    tags: ['LAG', 'PARTITION BY', 'ORDER BY'],
    explanation: 'LAG(created_at, 1)은 1행 이전, LAG(created_at, 2)는 2행 이전 값을 가져옵니다. 두 번째 인자로 오프셋을 지정할 수 있습니다.',
    relatedConceptTags: ['window-function'],
    sqlTemplate: 'SELECT ad_id, user_id, created_at,\n  LAG(created_at, ___)\n    OVER (PARTITION BY ad_id ORDER BY created_at) AS prev_1,\n  LAG(created_at, ___)\n    OVER (PARTITION BY ad_id ORDER BY created_at) AS prev_2\nFROM ad_logs;',
    blanks: 2,
    options: ['1', '2', '0', '-1', 'PRECEDING', 'CURRENT ROW', 'UNBOUNDED'],
    optionExplanations: [
      { value: '1', explanation: '1행 이전 값을 가져옵니다. LAG의 기본값이기도 합니다' },
      { value: '2', explanation: '2행 이전 값을 가져옵니다' },
      { value: '0', explanation: '0을 지정하면 현재 행의 값을 반환합니다. 이전 값이 아닙니다' },
      { value: '-1', explanation: '음수 오프셋은 허용되지 않습니다. LEAD를 사용하세요' },
      { value: 'PRECEDING', explanation: 'ROWS BETWEEN 프레임 지정에 사용되는 키워드입니다. LAG 인자로 사용할 수 없습니다' },
      { value: 'CURRENT ROW', explanation: 'ROWS BETWEEN 프레임 지정에 사용되는 키워드입니다. LAG 인자로 사용할 수 없습니다' },
      { value: 'UNBOUNDED', explanation: 'ROWS BETWEEN 프레임 지정에 사용되는 키워드입니다. LAG 인자로 사용할 수 없습니다' },
    ],
    correctAnswers: ['1', '2'],
    hints: {
      directional: ['LAG의 두 번째 인자는 몇 행 이전 값을 가져올지 숫자로 지정합니다'],
      constraint: ['첫 번째는 1행 이전, 두 번째는 2행 이전 값이 필요합니다'],
      misconception: ['음수 오프셋은 사용할 수 없습니다. 다음 행을 원하면 LEAD를 사용하세요'],
    },
  } as FillProblem,
  {
    id: 'swf11',
    category: 'sql-window',
    difficulty: 'intermediate',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '구독 플랜별 결제 금액 순위와 전체 대비 비율',
    question: '아래 SQL의 빈칸을 채워 subscriptions 테이블에서 플랜(plan_id)별로 결제 금액(amount) 내림차순 RANK를 구하고, 파티션 내 전체 합계도 함께 표시하세요.',
    learningPoint: '같은 OVER 절 조건을 가진 여러 윈도우 함수를 한 쿼리에 함께 사용할 수 있다',
    tags: ['RANK', 'SUM', 'PARTITION BY', 'ORDER BY'],
    explanation: '하나의 SELECT에서 RANK()와 SUM() OVER를 함께 사용할 수 있습니다. 각각 PARTITION BY plan_id로 그룹을 나눠 RANK는 순위를, SUM은 플랜 내 총합을 반환합니다.',
    relatedConceptTags: ['window-function'],
    sqlTemplate: 'SELECT user_id, plan_id, amount,\n  RANK() OVER (___ plan_id ORDER BY amount DESC) AS rnk,\n  ___(amount) OVER (PARTITION BY plan_id) AS plan_total\nFROM subscriptions;',
    blanks: 2,
    options: ['PARTITION BY', 'ORDER BY', 'SUM', 'AVG', 'GROUP BY', 'COUNT', 'HAVING'],
    optionExplanations: [
      { value: 'PARTITION BY', explanation: 'OVER 절 안에서 그룹을 나눕니다. RANK() 앞 OVER에 필요합니다' },
      { value: 'ORDER BY', explanation: '정렬 기준을 지정합니다. 이미 RANK() OVER에 있으므로 두 번째 빈칸은 집계 함수입니다' },
      { value: 'SUM', explanation: '그룹 내 합계를 구하는 집계 함수입니다. OVER와 함께 윈도우 집계로 동작합니다' },
      { value: 'AVG', explanation: '평균을 구합니다. 합계가 아닌 평균을 원할 때 사용합니다' },
      { value: 'GROUP BY', explanation: '집계 함수와 함께 그룹핑하는 절입니다. OVER 절 내에서 사용할 수 없습니다' },
      { value: 'COUNT', explanation: '행 수를 셉니다. 금액 합계와는 다릅니다' },
      { value: 'HAVING', explanation: '집계 결과를 필터링합니다. OVER 절 내에서 사용할 수 없습니다' },
    ],
    correctAnswers: ['PARTITION BY', 'SUM'],
    hints: {
      directional: ['RANK() OVER 뒤 그룹 분리 키워드와 합계 집계 함수를 찾으세요'],
      constraint: ['두 번째 빈칸은 amount의 합계를 구하는 집계 함수입니다'],
      misconception: ['GROUP BY와 PARTITION BY는 다릅니다. 윈도우 함수 안에서는 PARTITION BY를 사용하세요'],
    },
  } as FillProblem,
  {
    id: 'swf12',
    category: 'sql-window',
    difficulty: 'advanced',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '이커머스 이동 평균으로 주문 트렌드 분석',
    question: '아래 SQL의 빈칸을 채워 orders 테이블에서 created_at 기준으로 현재 행 포함 앞뒤 1행씩(총 3행) 이동 평균 금액을 구하세요.',
    learningPoint: 'ROWS BETWEEN N PRECEDING AND N FOLLOWING으로 이동 평균 윈도우를 설정할 수 있다',
    tags: ['AVG', 'ROWS BETWEEN', 'PRECEDING', 'FOLLOWING'],
    explanation: 'AVG(amount) OVER (ORDER BY created_at ROWS BETWEEN 1 PRECEDING AND 1 FOLLOWING)은 현재 행 앞뒤 1행씩 포함해 총 3행의 이동 평균을 계산합니다.',
    relatedConceptTags: ['window-function'],
    sqlTemplate: 'SELECT order_id, user_id, amount, created_at,\n  AVG(amount)\n  OVER (\n    ORDER BY created_at\n    ROWS BETWEEN ___ AND ___\n  ) AS moving_avg\nFROM orders;',
    blanks: 2,
    options: ['1 PRECEDING', '1 FOLLOWING', 'CURRENT ROW', 'UNBOUNDED PRECEDING', 'UNBOUNDED FOLLOWING', '2 PRECEDING', '0 PRECEDING'],
    optionExplanations: [
      { value: '1 PRECEDING', explanation: '현재 행 기준 1행 이전을 프레임 시작으로 지정합니다' },
      { value: '1 FOLLOWING', explanation: '현재 행 기준 1행 이후를 프레임 끝으로 지정합니다' },
      { value: 'CURRENT ROW', explanation: '현재 행을 프레임 경계로 지정합니다. 앞뒤 1행을 포함하지 않습니다' },
      { value: 'UNBOUNDED PRECEDING', explanation: '파티션의 첫 행부터 시작합니다. 이동 평균이 아닌 누적 평균이 됩니다' },
      { value: 'UNBOUNDED FOLLOWING', explanation: '파티션의 마지막 행까지 포함합니다. 고정된 이동 평균 범위가 아닙니다' },
      { value: '2 PRECEDING', explanation: '2행 이전부터 시작합니다. 3행이 아닌 더 많은 행이 포함됩니다' },
      { value: '0 PRECEDING', explanation: 'CURRENT ROW와 동일합니다. 앞 행을 포함하지 않습니다' },
    ],
    correctAnswers: ['1 PRECEDING', '1 FOLLOWING'],
    hints: {
      directional: ['현재 행 앞 1행부터 뒤 1행까지 범위를 지정하세요'],
      constraint: ['ROWS BETWEEN [시작] AND [끝] 형식으로 각각 PRECEDING/FOLLOWING을 사용합니다'],
      misconception: ['UNBOUNDED는 파티션 전체를 의미합니다. 이동 평균에는 구체적인 행 범위를 지정하세요'],
    },
  } as FillProblem,
  {
    id: 'swf13',
    category: 'sql-window',
    difficulty: 'advanced',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '스트리밍 시청 완료율 순위와 누적 집계 동시 적용',
    question: '아래 SQL의 빈칸을 채워 watch_logs 테이블에서 content_id별로 completed 기준 DENSE_RANK와 started_at 순 누적 시청 시간 합계를 동시에 구하세요.',
    learningPoint: '다른 OVER 조건을 가진 윈도우 함수들을 하나의 쿼리에서 동시에 사용할 수 있다',
    tags: ['DENSE_RANK', 'SUM', 'PARTITION BY', 'ORDER BY'],
    explanation: 'DENSE_RANK()는 PARTITION BY content_id ORDER BY completed DESC로, SUM()은 PARTITION BY content_id ORDER BY started_at으로 각각 다른 OVER 조건을 가질 수 있습니다.',
    relatedConceptTags: ['window-function'],
    sqlTemplate: 'SELECT user_id, content_id, duration_sec, completed, started_at,\n  ___()\n    OVER (PARTITION BY content_id ORDER BY completed DESC) AS completion_rank,\n  SUM(duration_sec)\n    OVER (___ content_id ___ started_at) AS cumulative_duration\nFROM watch_logs;',
    blanks: 3,
    options: ['DENSE_RANK', 'RANK', 'ROW_NUMBER', 'PARTITION BY', 'ORDER BY', 'GROUP BY', 'NTILE'],
    optionExplanations: [
      { value: 'DENSE_RANK', explanation: '순위를 건너뛰지 않는 순위 함수입니다. 동일 completed 값에 같은 순위를 부여합니다' },
      { value: 'RANK', explanation: '동일 값에 같은 순위를 부여하지만 다음 순위를 건너뜁니다' },
      { value: 'ROW_NUMBER', explanation: '항상 고유한 순번을 부여합니다. 동일 completed 값도 다른 번호가 매겨집니다' },
      { value: 'PARTITION BY', explanation: 'OVER 절 안에서 그룹을 나눕니다. SUM OVER의 content_id 앞에 필요합니다' },
      { value: 'ORDER BY', explanation: '정렬 기준을 지정합니다. SUM OVER의 started_at 앞에 필요합니다' },
      { value: 'GROUP BY', explanation: '집계 함수 그룹핑 절입니다. OVER 절 내에서 사용할 수 없습니다' },
      { value: 'NTILE', explanation: '행을 N개 버킷으로 나누는 함수입니다. 순위 함수와는 다릅니다' },
    ],
    correctAnswers: ['DENSE_RANK', 'PARTITION BY', 'ORDER BY'],
    hints: {
      directional: ['순위를 건너뛰지 않는 순위 함수, 그룹 분리 키워드, 정렬 키워드를 순서대로 찾으세요'],
      constraint: ['두 번째·세 번째 빈칸은 SUM OVER 절 안 PARTITION BY와 ORDER BY입니다'],
      misconception: ['RANK는 순위를 건너뜁니다. 건너뛰지 않으려면 DENSE_RANK를 사용하세요'],
    },
  } as FillProblem,
  {
    id: 'swf14',
    category: 'sql-window',
    difficulty: 'advanced',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '배달 음식점별 최근 배달과 첫 배달 시간 비교',
    question: '아래 SQL의 빈칸을 채워 deliveries 테이블에서 음식점(restaurant_id)별로 첫 번째 배달 시간(FIRST_VALUE)과 마지막 배달 시간(LAST_VALUE)을 함께 표시하세요. LAST_VALUE는 전체 파티션을 프레임으로 사용하세요.',
    learningPoint: 'FIRST_VALUE와 LAST_VALUE를 함께 쓸 때 LAST_VALUE에는 프레임 범위를 명시해야 한다',
    tags: ['FIRST_VALUE', 'LAST_VALUE', 'ROWS BETWEEN', 'PARTITION BY'],
    explanation: 'FIRST_VALUE는 기본 프레임으로 올바르게 동작하지만 LAST_VALUE는 기본 프레임이 CURRENT ROW까지라 전체 파티션의 마지막 값을 가져오려면 ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING이 필요합니다.',
    relatedConceptTags: ['window-function'],
    sqlTemplate: 'SELECT order_id, restaurant_id, delivered_at,\n  FIRST_VALUE(delivered_at)\n    OVER (PARTITION BY restaurant_id ORDER BY delivered_at) AS first_delivery,\n  ___(delivered_at)\n    OVER (PARTITION BY restaurant_id ORDER BY delivered_at\n          ___ UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING) AS last_delivery\nFROM deliveries;',
    blanks: 2,
    options: ['LAST_VALUE', 'FIRST_VALUE', 'MAX', 'ROWS BETWEEN', 'RANGE BETWEEN', 'LEAD', 'LAG'],
    optionExplanations: [
      { value: 'LAST_VALUE', explanation: '윈도우 프레임의 마지막 행 값을 반환합니다. 전체 파티션을 보려면 프레임 명시가 필요합니다' },
      { value: 'FIRST_VALUE', explanation: '첫 번째 행 값을 반환합니다. 이미 위에서 사용했으므로 여기는 마지막 값 함수가 필요합니다' },
      { value: 'MAX', explanation: '최댓값을 반환합니다. 날짜의 최댓값이 마지막 배달이 될 수 있지만 LAST_VALUE가 더 의미적으로 명확합니다' },
      { value: 'ROWS BETWEEN', explanation: '물리적 행 기준 프레임을 지정합니다. UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING과 함께 전체 파티션을 지정합니다' },
      { value: 'RANGE BETWEEN', explanation: '논리적 값 범위 기준 프레임을 지정합니다. ROWS BETWEEN과 동작이 다를 수 있습니다' },
      { value: 'LEAD', explanation: '다음 행 값을 반환합니다. 마지막 배달 시간이 아닙니다' },
      { value: 'LAG', explanation: '이전 행 값을 반환합니다. 마지막 배달 시간이 아닙니다' },
    ],
    correctAnswers: ['LAST_VALUE', 'ROWS BETWEEN'],
    hints: {
      directional: ['마지막 행 값을 반환하는 함수와 프레임 범위 지정 키워드를 찾으세요'],
      constraint: ['LAST_VALUE가 전체 파티션 마지막 값을 반환하려면 ROWS BETWEEN ... UNBOUNDED FOLLOWING이 필요합니다'],
      misconception: ['LAST_VALUE의 기본 프레임은 CURRENT ROW까지입니다. 프레임을 명시하지 않으면 현재 행 값이 반환될 수 있습니다'],
    },
  } as FillProblem,
  {
    id: 'swf15',
    category: 'sql-window',
    difficulty: 'advanced',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '구독 사용자별 이전 플랜과 현재 플랜 금액 차이 분석',
    question: '아래 SQL의 빈칸을 채워 subscriptions 테이블에서 사용자(user_id)별로 이전 구독 금액(LAG)과 현재 금액의 차이를 계산하세요. LAG의 기본값(세 번째 인자)은 0으로 설정하세요.',
    learningPoint: 'LAG(col, offset, default)의 세 번째 인자로 이전 값이 없을 때의 기본값을 지정할 수 있다',
    tags: ['LAG', 'PARTITION BY', 'ORDER BY'],
    explanation: 'LAG(amount, 1, 0)은 이전 행의 amount를 가져오되, 이전 행이 없으면 0을 반환합니다. 이를 이용해 (amount - LAG(amount, 1, 0))으로 금액 변화량을 계산할 수 있습니다.',
    relatedConceptTags: ['window-function'],
    sqlTemplate: 'SELECT user_id, plan_id, amount, start_date,\n  LAG(amount, ___, ___)\n    OVER (PARTITION BY user_id ORDER BY start_date) AS prev_amount,\n  amount - LAG(amount, 1, 0)\n    OVER (PARTITION BY user_id ORDER BY start_date) AS amount_diff\nFROM subscriptions;',
    blanks: 2,
    options: ['1', '0', 'NULL', '2', '-1', 'PRECEDING', 'UNBOUNDED'],
    optionExplanations: [
      { value: '1', explanation: '1행 이전 값을 가져옵니다. LAG의 두 번째 인자(오프셋)입니다' },
      { value: '0', explanation: '이전 행이 없을 때 반환할 기본값입니다. NULL 대신 0을 반환하게 합니다' },
      { value: 'NULL', explanation: 'LAG의 기본값입니다. 별도로 지정하면 이전 값이 없을 때 NULL을 반환합니다. 문제에서는 0을 요구합니다' },
      { value: '2', explanation: '2행 이전 값을 가져옵니다. 바로 이전 행이 필요하므로 1을 사용합니다' },
      { value: '-1', explanation: '음수 오프셋은 허용되지 않습니다. 다음 행은 LEAD를 사용하세요' },
      { value: 'PRECEDING', explanation: 'ROWS BETWEEN 프레임 지정 키워드입니다. LAG 인자로 사용할 수 없습니다' },
      { value: 'UNBOUNDED', explanation: 'ROWS BETWEEN 프레임 지정 키워드입니다. LAG 인자로 사용할 수 없습니다' },
    ],
    correctAnswers: ['1', '0'],
    hints: {
      directional: ['LAG의 두 번째 인자는 몇 행 이전인지, 세 번째 인자는 이전 값이 없을 때의 기본값입니다'],
      constraint: ['바로 이전 1행, 없을 때는 0을 반환해야 합니다'],
      misconception: ['기본값을 지정하지 않으면 NULL이 반환됩니다. 산술 연산 시 NULL이 포함되면 결과도 NULL이 됩니다'],
    },
  } as FillProblem,
  {
    id: 'swf16',
    category: 'sql-window',
    difficulty: 'advanced',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '이커머스 카테고리별 상위 3개 상품 필터링',
    question: '아래 SQL의 빈칸을 채워 products 테이블에서 카테고리(category)별 가격(price) 기준 상위 3개 상품을 ROW_NUMBER로 필터링하세요.',
    learningPoint: '서브쿼리에서 ROW_NUMBER()로 순번을 매긴 뒤 외부 WHERE로 필터링하는 패턴',
    tags: ['ROW_NUMBER', 'PARTITION BY', 'ORDER BY'],
    explanation: 'ROW_NUMBER() OVER (PARTITION BY category ORDER BY price DESC)로 카테고리별 가격 순번을 매기고, 외부 쿼리에서 WHERE rn <= 3으로 상위 3개를 필터링합니다. 윈도우 함수는 WHERE 절에서 직접 참조할 수 없어 서브쿼리가 필요합니다.',
    relatedConceptTags: ['window-function'],
    sqlTemplate: 'SELECT product_id, category, price, rn\nFROM (\n  SELECT product_id, category, price,\n    ___()\n    OVER (___ category ORDER BY price DESC) AS rn\n  FROM products\n) ranked\nWHERE ___ <= 3;',
    blanks: 3,
    options: ['ROW_NUMBER', 'RANK', 'DENSE_RANK', 'PARTITION BY', 'ORDER BY', 'rn', 'price'],
    optionExplanations: [
      { value: 'ROW_NUMBER', explanation: '고유한 순번을 부여합니다. 카테고리별 정확히 3개를 뽑을 때 적합합니다' },
      { value: 'RANK', explanation: '동점 시 같은 순위를 부여합니다. rn <= 3 필터 시 동점자가 여럿이면 3개 이상 반환될 수 있습니다' },
      { value: 'DENSE_RANK', explanation: '순위를 건너뛰지 않습니다. 동점자 존재 시 3개 이상 반환될 수 있습니다' },
      { value: 'PARTITION BY', explanation: 'OVER 절 안에서 카테고리별로 그룹을 나눕니다' },
      { value: 'ORDER BY', explanation: '이미 ORDER BY가 있으므로 이 위치는 PARTITION BY입니다' },
      { value: 'rn', explanation: '서브쿼리에서 만든 ROW_NUMBER 별칭입니다. WHERE 절에서 이 값으로 필터링합니다' },
      { value: 'price', explanation: '가격 컬럼입니다. 순번 필터링에는 rn을 사용해야 합니다' },
    ],
    correctAnswers: ['ROW_NUMBER', 'PARTITION BY', 'rn'],
    hints: {
      directional: ['고유 순번 함수, 그룹 분리 키워드, WHERE 절의 필터링 컬럼명을 순서대로 찾으세요'],
      constraint: ['정확히 카테고리별 3개만 뽑으려면 ROW_NUMBER가 적합합니다'],
      misconception: ['윈도우 함수 결과를 WHERE에서 바로 쓸 수 없습니다. 서브쿼리로 감싼 뒤 외부 WHERE에서 참조하세요'],
    },
  } as FillProblem,
  // ── swo02 ~ swo15 ──────────────────────────────────────────────
  {
    id: 'swo02',
    category: 'sql-window',
    difficulty: 'basic',
    type: 'ox',
    availableModes: ['write', 'fill'] as const,
    title: '윈도우 함수와 행 수 유지',
    question: '윈도우 함수를 사용하면 GROUP BY처럼 결과 행 수가 줄어드는지 확인합니다.',
    learningPoint: '윈도우 함수는 행을 집계해도 원본 행 수를 유지한다',
    tags: ['윈도우함수', 'GROUP BY', '행수'],
    explanation: '윈도우 함수(OVER 절 포함)는 GROUP BY와 달리 결과 행 수를 줄이지 않습니다. 각 행마다 계산 결과를 추가로 제공하면서 원본 행이 그대로 유지됩니다.',
    relatedConceptTags: ['window-function'],
    statement: 'SELECT department_id, SUM(salary) OVER (PARTITION BY department_id) FROM employees; 이 쿼리는 부서 수만큼의 행을 반환한다.',
    answer: 'X',
    hints: {
      directional: ['OVER 절이 있는 SUM()은 GROUP BY와 다르게 동작합니다'],
      misconception: ['SUM() OVER()는 집계 함수처럼 보이지만 원본 행 수를 그대로 유지합니다. 부서별 합계를 각 행에 추가할 뿐입니다'],
    },
  } as OxProblem,
  {
    id: 'swo03',
    category: 'sql-window',
    difficulty: 'basic',
    type: 'ox',
    availableModes: ['write', 'fill'] as const,
    title: 'PARTITION BY 생략 가능 여부',
    question: 'OVER 절에서 PARTITION BY를 생략할 수 있는지 확인합니다.',
    learningPoint: 'PARTITION BY는 선택 사항이며 생략 시 전체 테이블이 하나의 윈도우',
    tags: ['PARTITION BY', 'OVER'],
    explanation: 'PARTITION BY는 생략 가능합니다. 생략하면 OVER()가 되어 전체 테이블이 하나의 파티션으로 처리됩니다. 예: SUM(salary) OVER()는 전체 직원 급여 합계를 각 행에 보여줍니다.',
    relatedConceptTags: ['window-function'],
    statement: 'OVER() 처럼 PARTITION BY 없이 OVER 절을 사용하면 문법 오류가 발생한다.',
    answer: 'X',
    hints: {
      directional: ['OVER() 구문이 유효한지 생각해보세요'],
      misconception: ['PARTITION BY는 선택사항입니다. OVER()만 써도 유효하며 전체 테이블 범위로 계산됩니다'],
    },
  } as OxProblem,
  {
    id: 'swo04',
    category: 'sql-window',
    difficulty: 'basic',
    type: 'ox',
    availableModes: ['write', 'fill'] as const,
    title: 'DENSE_RANK 순위 연속성',
    question: 'DENSE_RANK()가 동일 순위 이후 순위를 어떻게 매기는지 확인합니다.',
    learningPoint: 'DENSE_RANK는 동일 순위 다음 순위를 건너뛰지 않는다',
    tags: ['DENSE_RANK', 'RANK'],
    explanation: 'DENSE_RANK()는 동일 값에 같은 순위를 부여하되, 다음 순위를 건너뛰지 않습니다. 예: 1,1,2,3. RANK()는 1,1,3,4 처럼 건너뜁니다.',
    relatedConceptTags: ['window-function'],
    statement: '급여 순위에서 1위가 2명일 때 DENSE_RANK()를 사용하면 그 다음 순위는 2위가 된다.',
    answer: 'O',
    hints: {
      directional: ['DENSE_RANK의 DENSE(빽빽한)라는 의미를 생각해보세요'],
      misconception: ['RANK()는 1,1,3 이지만 DENSE_RANK()는 1,1,2 입니다. 순위가 연속적으로 이어집니다'],
    },
  } as OxProblem,
  {
    id: 'swo05',
    category: 'sql-window',
    difficulty: 'basic',
    type: 'ox',
    availableModes: ['write', 'fill'] as const,
    title: 'LAG 함수의 방향',
    question: 'LAG 함수가 이전 행과 다음 행 중 어느 쪽을 참조하는지 확인합니다.',
    learningPoint: 'LAG()는 현재 행보다 이전(앞선) 행의 값을 가져온다',
    tags: ['LAG', 'LEAD'],
    explanation: 'LAG()는 현재 행 기준으로 이전 행(앞쪽)의 값을 가져옵니다. 반대로 LEAD()는 다음 행(뒤쪽)의 값을 가져옵니다. 이름의 LAG=지연(뒤처짐)으로 기억하면 됩니다.',
    relatedConceptTags: ['window-function'],
    statement: 'LAG(salary, 1) OVER (ORDER BY hire_date)는 현재 행보다 나중에 입사한 직원의 급여를 반환한다.',
    answer: 'X',
    hints: {
      directional: ['LAG와 LEAD 중 어느 것이 이전/다음 행을 참조하는지 생각해보세요'],
      misconception: ['LAG는 이전 행, LEAD는 다음 행입니다. hire_date 오름차순이면 LAG는 더 일찍 입사한 직원의 급여를 반환합니다'],
    },
  } as OxProblem,
  {
    id: 'swo06',
    category: 'sql-window',
    difficulty: 'basic',
    type: 'ox',
    availableModes: ['write', 'fill'] as const,
    title: 'WHERE 절에서 윈도우 함수 사용',
    question: '윈도우 함수를 WHERE 절에서 직접 사용할 수 있는지 확인합니다.',
    learningPoint: '윈도우 함수는 WHERE 절에 직접 사용할 수 없고 서브쿼리나 CTE가 필요하다',
    tags: ['WHERE', '윈도우함수', '서브쿼리'],
    explanation: '윈도우 함수는 SELECT 절에서만 사용 가능합니다. WHERE 절에서 직접 사용하면 오류가 발생합니다. 필터링이 필요하면 서브쿼리나 CTE로 감싸야 합니다.',
    relatedConceptTags: ['window-function'],
    statement: 'WHERE RANK() OVER (ORDER BY salary DESC) = 1 처럼 WHERE 절에서 윈도우 함수를 직접 사용할 수 있다.',
    answer: 'X',
    hints: {
      directional: ['SQL의 실행 순서(WHERE → SELECT)를 생각해보세요'],
      misconception: ['WHERE 절은 SELECT 절보다 먼저 실행되므로 윈도우 함수 결과를 참조할 수 없습니다. WITH 절(CTE)이나 서브쿼리를 사용해야 합니다'],
    },
  } as OxProblem,
  {
    id: 'swo07',
    category: 'sql-window',
    difficulty: 'intermediate',
    type: 'ox',
    availableModes: ['write', 'fill'] as const,
    title: 'ORDER BY 없는 SUM OVER의 결과',
    question: 'OVER 절에 ORDER BY가 없는 SUM()의 결과가 누적합인지 전체합인지 확인합니다.',
    learningPoint: 'ORDER BY 없는 SUM() OVER()는 전체 합계를 각 행에 반환한다',
    tags: ['SUM', 'OVER', 'ORDER BY', '누적합'],
    explanation: 'SUM(salary) OVER()처럼 ORDER BY가 없으면 파티션 전체 합계를 각 행에 동일하게 반환합니다. SUM(salary) OVER(ORDER BY id)처럼 ORDER BY가 있으면 현재 행까지의 누적합을 반환합니다.',
    relatedConceptTags: ['window-function'],
    statement: 'SUM(salary) OVER (PARTITION BY department_id)는 부서별 급여 누적합(행이 늘어날수록 증가)을 반환한다.',
    answer: 'X',
    hints: {
      directional: ['ORDER BY 유무에 따른 SUM() OVER()의 동작 차이를 생각해보세요'],
      misconception: ['ORDER BY가 없으면 파티션 내 전체 합계가 모든 행에 동일하게 나타납니다. 누적합이 되려면 ORDER BY가 필요합니다'],
    },
  } as OxProblem,
  {
    id: 'swo08',
    category: 'sql-window',
    difficulty: 'intermediate',
    type: 'ox',
    availableModes: ['write', 'fill'] as const,
    title: 'OVER의 ORDER BY와 쿼리의 ORDER BY 독립성',
    question: 'OVER 절의 ORDER BY와 최종 결과의 ORDER BY가 서로 영향을 주는지 확인합니다.',
    learningPoint: 'OVER 내부의 ORDER BY는 윈도우 계산용이며 결과 정렬과 독립적이다',
    tags: ['ORDER BY', 'OVER'],
    explanation: 'OVER(ORDER BY salary)는 윈도우 함수 계산을 위한 정렬 기준이며, 최종 결과 행의 순서와는 무관합니다. 결과를 정렬하려면 쿼리 끝에 별도의 ORDER BY를 사용해야 합니다.',
    relatedConceptTags: ['window-function'],
    statement: 'OVER(ORDER BY salary DESC)를 사용하면 최종 쿼리 결과도 salary 내림차순으로 정렬된다.',
    answer: 'X',
    hints: {
      directional: ['OVER 내부 ORDER BY가 최종 결과 순서에 영향을 주는지 생각해보세요'],
      misconception: ['OVER 절 내부의 ORDER BY는 윈도우 함수 계산용 정렬입니다. 결과 정렬을 원하면 쿼리 마지막에 ORDER BY를 별도로 써야 합니다'],
    },
  } as OxProblem,
  {
    id: 'swo09',
    category: 'sql-window',
    difficulty: 'intermediate',
    type: 'ox',
    availableModes: ['write', 'fill'] as const,
    title: 'NTILE 분포와 나머지 처리',
    question: 'NTILE(n)으로 균등 분배할 때 나머지 행이 어느 버킷에 추가되는지 확인합니다.',
    learningPoint: 'NTILE에서 나머지 행은 앞쪽 버킷부터 하나씩 추가된다',
    tags: ['NTILE'],
    explanation: 'NTILE(4)에서 행이 10개면 10 ÷ 4 = 2 나머지 2입니다. 앞쪽 버킷 2개(1번, 2번)가 3행씩, 나머지 버킷(3번, 4번)이 2행씩 배분됩니다.',
    relatedConceptTags: ['window-function'],
    statement: 'NTILE(4)로 11개 행을 분배할 때 모든 버킷은 동일한 수의 행을 가진다.',
    answer: 'X',
    hints: {
      directional: ['11 ÷ 4 나눗셈에서 나머지가 발생하는지 생각해보세요'],
      misconception: ['행 수가 버킷 수로 나누어 떨어지지 않으면 앞쪽 버킷들이 1개씩 더 받습니다. 11개라면 1,2,3번 버킷은 3행, 4번 버킷은 2행이 됩니다'],
    },
  } as OxProblem,
  {
    id: 'swo10',
    category: 'sql-window',
    difficulty: 'intermediate',
    type: 'ox',
    availableModes: ['write', 'fill'] as const,
    title: 'LAST_VALUE와 프레임 기본값',
    question: 'LAST_VALUE()가 파티션의 마지막 값을 올바르게 반환하는지 기본 프레임 설정으로 확인합니다.',
    learningPoint: 'LAST_VALUE는 기본 프레임이 현재 행까지여서 의도한 마지막 값을 얻으려면 ROWS BETWEEN 명시가 필요하다',
    tags: ['LAST_VALUE', 'ROWS BETWEEN', '프레임'],
    explanation: 'ORDER BY가 있을 때 기본 프레임은 RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW입니다. LAST_VALUE()는 이 프레임의 마지막(현재 행)을 반환하므로 파티션 전체의 마지막 값을 얻으려면 ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING을 명시해야 합니다.',
    relatedConceptTags: ['window-function'],
    statement: 'ORDER BY가 포함된 OVER 절에서 LAST_VALUE()를 사용하면 기본적으로 파티션의 마지막 행 값을 반환한다.',
    answer: 'X',
    hints: {
      directional: ['ORDER BY가 있을 때 윈도우 프레임의 기본 종료 경계가 어디인지 생각해보세요'],
      misconception: ['기본 프레임 끝은 CURRENT ROW입니다. 따라서 LAST_VALUE()는 현재 행 자신을 반환합니다. 파티션 끝 값을 원하면 ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING을 명시하세요'],
    },
  } as OxProblem,
  {
    id: 'swo11',
    category: 'sql-window',
    difficulty: 'intermediate',
    type: 'ox',
    availableModes: ['write', 'fill'] as const,
    title: 'LEAD의 기본 오프셋',
    question: 'LEAD(column)처럼 오프셋을 생략했을 때 몇 행 뒤를 참조하는지 확인합니다.',
    learningPoint: 'LAG/LEAD의 기본 오프셋은 1이다',
    tags: ['LEAD', 'LAG', '오프셋'],
    explanation: 'LAG(salary)와 LAG(salary, 1)은 동일합니다. 오프셋 기본값은 1이므로 직전(또는 직후) 행을 참조합니다. 2행 전후를 원하면 LAG(salary, 2)처럼 명시해야 합니다.',
    relatedConceptTags: ['window-function'],
    statement: 'LEAD(salary) OVER (ORDER BY hire_date)는 LEAD(salary, 1) OVER (ORDER BY hire_date)와 동일한 결과를 반환한다.',
    answer: 'O',
    hints: {
      directional: ['오프셋 인자를 생략했을 때 기본값이 무엇인지 생각해보세요'],
      misconception: ['오프셋을 생략하면 기본값 1이 적용됩니다. LEAD(salary)와 LEAD(salary, 1)은 완전히 동일합니다'],
    },
  } as OxProblem,
  {
    id: 'swo12',
    category: 'sql-window',
    difficulty: 'advanced',
    type: 'ox',
    availableModes: ['write', 'fill'] as const,
    title: 'ROWS BETWEEN과 RANGE BETWEEN의 차이',
    question: 'ROWS BETWEEN과 RANGE BETWEEN이 동일 값을 다루는 방식의 차이를 확인합니다.',
    learningPoint: 'RANGE BETWEEN은 동일 값 행을 하나의 그룹으로 처리하고 ROWS BETWEEN은 물리적 행 단위로 처리한다',
    tags: ['ROWS BETWEEN', 'RANGE BETWEEN', '프레임'],
    explanation: 'ROWS BETWEEN CURRENT ROW AND 1 FOLLOWING은 현재 행과 바로 다음 물리적 행 2개를 포함합니다. RANGE BETWEEN은 ORDER BY 컬럼 값이 같은 모든 행을 동일 범위로 취급합니다. 따라서 같은 값이 여러 행이면 RANGE가 더 많은 행을 포함할 수 있습니다.',
    relatedConceptTags: ['window-function'],
    statement: 'ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW와 RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW는 ORDER BY 컬럼에 중복 값이 없다면 항상 동일한 결과를 반환한다.',
    answer: 'O',
    hints: {
      directional: ['ORDER BY 컬럼에 중복이 없는 특수한 조건을 고려하세요'],
      misconception: ['중복 값이 없으면 RANGE와 ROWS는 동일하게 동작합니다. 차이는 중복 값이 존재할 때 나타납니다'],
    },
  } as OxProblem,
  {
    id: 'swo13',
    category: 'sql-window',
    difficulty: 'advanced',
    type: 'ox',
    availableModes: ['write', 'fill'] as const,
    title: '윈도우 함수와 HAVING 절',
    question: '윈도우 함수를 HAVING 절에서 직접 사용할 수 있는지 확인합니다.',
    learningPoint: '윈도우 함수는 HAVING 절에서도 직접 사용할 수 없다',
    tags: ['HAVING', '윈도우함수', 'CTE'],
    explanation: 'HAVING 절은 GROUP BY 집계 결과를 필터링하는 용도입니다. 윈도우 함수는 SELECT 처리 단계에서 계산되므로 HAVING 절에서 직접 참조할 수 없습니다. CTE나 서브쿼리로 감싸야 합니다.',
    relatedConceptTags: ['window-function'],
    statement: 'HAVING RANK() OVER (ORDER BY SUM(salary) DESC) <= 3 처럼 윈도우 함수를 HAVING 절에 직접 사용할 수 있다.',
    answer: 'X',
    hints: {
      directional: ['SQL 논리 실행 순서에서 HAVING과 SELECT의 순서를 생각해보세요'],
      misconception: ['HAVING은 SELECT 이전에 처리되므로 윈도우 함수 결과를 참조할 수 없습니다. WITH(CTE)나 서브쿼리로 윈도우 함수 결과를 먼저 만든 후 필터링해야 합니다'],
    },
  } as OxProblem,
  {
    id: 'swo14',
    category: 'sql-window',
    difficulty: 'advanced',
    type: 'ox',
    availableModes: ['write', 'fill'] as const,
    title: 'FIRST_VALUE의 프레임 안정성',
    question: 'FIRST_VALUE()가 프레임 설정과 무관하게 파티션의 첫 번째 값을 반환하는지 확인합니다.',
    learningPoint: 'FIRST_VALUE도 프레임 범위에 영향을 받으며 기본 프레임 시작은 UNBOUNDED PRECEDING이라 첫 값은 안정적이다',
    tags: ['FIRST_VALUE', '프레임', 'UNBOUNDED PRECEDING'],
    explanation: 'FIRST_VALUE()는 프레임의 첫 번째 행 값을 반환합니다. 기본 프레임 시작이 UNBOUNDED PRECEDING이므로 파티션 첫 행이 항상 포함됩니다. 따라서 FIRST_VALUE는 일반적으로 파티션 첫 값을 안정적으로 반환합니다. 반면 LAST_VALUE는 기본 프레임 끝이 CURRENT ROW라 문제가 생깁니다.',
    relatedConceptTags: ['window-function'],
    statement: 'ORDER BY가 포함된 OVER 절에서 FIRST_VALUE()는 기본 프레임으로도 파티션의 첫 번째 값을 올바르게 반환한다.',
    answer: 'O',
    hints: {
      directional: ['기본 프레임의 시작 경계가 어디인지 생각해보세요'],
      misconception: ['기본 프레임은 UNBOUNDED PRECEDING부터 시작하므로 FIRST_VALUE는 항상 파티션 시작부터 포함됩니다. LAST_VALUE와 달리 별도 프레임 지정이 필요 없습니다'],
    },
  } as OxProblem,
  {
    id: 'swo15',
    category: 'sql-window',
    difficulty: 'advanced',
    type: 'ox',
    availableModes: ['write', 'fill'] as const,
    title: '중첩 윈도우 함수 사용 가능 여부',
    question: '윈도우 함수 안에 다른 윈도우 함수를 중첩해서 사용할 수 있는지 확인합니다.',
    learningPoint: '윈도우 함수는 중첩 사용이 불가능하며 CTE나 서브쿼리로 분리해야 한다',
    tags: ['윈도우함수', '중첩', 'CTE'],
    explanation: 'SQL 표준과 대부분의 DBMS에서 윈도우 함수 안에 다른 윈도우 함수를 중첩하는 것은 허용되지 않습니다. RANK() OVER (ORDER BY SUM(salary) OVER (...)) 같은 형태는 오류를 발생시킵니다. 중간 결과가 필요하면 CTE나 서브쿼리로 단계를 분리해야 합니다.',
    relatedConceptTags: ['window-function'],
    statement: 'RANK() OVER (ORDER BY SUM(salary) OVER (PARTITION BY department_id)) 처럼 윈도우 함수를 중첩하여 사용할 수 있다.',
    answer: 'X',
    hints: {
      directional: ['윈도우 함수의 OVER 절 안에 또 다른 윈도우 함수가 올 수 있는지 생각해보세요'],
      misconception: ['윈도우 함수는 중첩이 불가능합니다. 부서별 급여합을 먼저 CTE로 계산한 뒤 그 결과에 RANK()를 적용하는 방식으로 분리해야 합니다'],
    },
  } as OxProblem,
  // ── swp01 ~ swp05 ──────────────────────────────────────────────
  {
    id: 'swp01',
    category: 'sql-window',
    difficulty: 'basic',
    type: 'plan',
    availableModes: ['write', 'fill'] as const,
    title: '이커머스 상품 가격 순위 실행 계획 (WINDOW SORT)',
    question: '아래 Oracle 실행 계획은 products 테이블에서 카테고리별 가격 순위를 구하는 쿼리의 실행 계획입니다. 이 실행 계획에서 "WINDOW SORT" 단계의 역할은 무엇입니까?',
    learningPoint: 'WINDOW SORT는 윈도우 함수 처리를 위해 ORDER BY 기준으로 데이터를 정렬하는 단계이다',
    tags: ['WINDOW SORT', 'ROW_NUMBER', 'PARTITION BY', 'ORDER BY'],
    explanation: 'WINDOW SORT는 Oracle이 윈도우 함수(ROW_NUMBER, RANK 등)를 처리하기 위해 OVER 절의 PARTITION BY + ORDER BY 기준으로 데이터를 정렬하는 단계입니다. 이 정렬 이후 각 파티션 내에서 순위가 계산됩니다.',
    relatedConceptTags: ['window-function', 'execution-plan'],
    planText: `------------------------------------------------------------
| Id | Operation          | Name     | Rows | Cost (%CPU)|
------------------------------------------------------------
|  0 | SELECT STATEMENT   |          |  500 |    12   (8)|
|  1 |  WINDOW SORT       |          |  500 |    12   (8)|
|  2 |   TABLE ACCESS FULL| PRODUCTS |  500 |    11   (0)|
------------------------------------------------------------

Query:
SELECT product_id, category, price,
  ROW_NUMBER() OVER (PARTITION BY category ORDER BY price DESC) AS rn
FROM products;`,
    choices: [
      'PARTITION BY category와 ORDER BY price DESC 기준으로 데이터를 정렬하여 윈도우 함수가 각 파티션 내 순위를 계산할 수 있도록 준비하는 단계',
      'TABLE ACCESS FULL로 읽어온 모든 행을 price 기준 오름차순으로 정렬하는 최종 출력용 정렬 단계',
      'GROUP BY와 동일하게 카테고리별로 행을 집계하여 한 행으로 압축하는 집계 단계',
      'PRODUCTS 테이블에 인덱스가 없을 때 Full Table Scan 대신 사용되는 소트-머지 단계',
    ],
    choiceExplanations: [
      { value: 'PARTITION BY category와 ORDER BY price DESC 기준으로 데이터를 정렬하여 윈도우 함수가 각 파티션 내 순위를 계산할 수 있도록 준비하는 단계', explanation: '정답입니다. WINDOW SORT는 OVER(PARTITION BY ... ORDER BY ...) 절의 기준에 따라 데이터를 정렬하여 이후 윈도우 계산을 가능하게 합니다.' },
      { value: 'TABLE ACCESS FULL로 읽어온 모든 행을 price 기준 오름차순으로 정렬하는 최종 출력용 정렬 단계', explanation: '오답입니다. 최종 출력 정렬은 SORT ORDER BY로 표시됩니다. WINDOW SORT는 윈도우 함수 처리를 위한 내부 정렬 단계입니다.' },
      { value: 'GROUP BY와 동일하게 카테고리별로 행을 집계하여 한 행으로 압축하는 집계 단계', explanation: '오답입니다. 윈도우 함수는 GROUP BY와 달리 원본 행 수를 유지합니다. WINDOW SORT는 정렬만 수행하며 행을 집계하거나 압축하지 않습니다.' },
      { value: 'PRODUCTS 테이블에 인덱스가 없을 때 Full Table Scan 대신 사용되는 소트-머지 단계', explanation: '오답입니다. WINDOW SORT는 인덱스 유무와 관계없이 윈도우 함수 처리를 위해 항상 발생하는 정렬 단계입니다.' },
    ],
    correctAnswer: 'PARTITION BY category와 ORDER BY price DESC 기준으로 데이터를 정렬하여 윈도우 함수가 각 파티션 내 순위를 계산할 수 있도록 준비하는 단계',
    hints: {
      directional: ['WINDOW SORT 단계가 어떤 OVER 절 정보를 사용하는지 생각해보세요'],
      constraint: ['실행 계획에서 WINDOW SORT는 윈도우 함수 전용 정렬 단계입니다'],
      misconception: ['WINDOW SORT는 GROUP BY 집계와 다릅니다. 행 수는 그대로 유지됩니다'],
    },
  } as PlanProblem,
  {
    id: 'swp02',
    category: 'sql-window',
    difficulty: 'basic',
    type: 'plan',
    availableModes: ['write', 'fill'] as const,
    title: '게임 유저 이전 점수 비교 실행 계획 (WINDOW BUFFER)',
    question: '아래 Oracle 실행 계획은 게임 scores 테이블에서 LAG 함수를 사용한 쿼리의 실행 계획입니다. "WINDOW BUFFER" 단계가 나타나는 이유는 무엇입니까?',
    learningPoint: 'WINDOW BUFFER는 LAG/LEAD처럼 인접 행을 참조하는 함수를 처리하기 위해 행을 버퍼에 보관하는 단계이다',
    tags: ['WINDOW BUFFER', 'LAG', 'LEAD', 'PARTITION BY'],
    explanation: 'LAG와 LEAD는 현재 행의 이전/다음 행 값을 참조합니다. Oracle은 이를 처리하기 위해 WINDOW BUFFER 단계에서 파티션 내 행들을 메모리 버퍼에 보관하여 인접 행 접근을 가능하게 합니다. 이는 단순 ORDER BY 정렬만으로는 인접 행 참조가 불가능하기 때문입니다.',
    relatedConceptTags: ['window-function', 'execution-plan'],
    planText: `------------------------------------------------------------
| Id | Operation          | Name   | Rows | Cost (%CPU)|
------------------------------------------------------------
|  0 | SELECT STATEMENT   |        | 1000 |    15   (7)|
|  1 |  WINDOW BUFFER     |        | 1000 |    15   (7)|
|  2 |   TABLE ACCESS FULL| SCORES | 1000 |    14   (0)|
------------------------------------------------------------

Query:
SELECT user_id, score, played_at,
  LAG(score) OVER (PARTITION BY user_id ORDER BY played_at) AS prev_score
FROM scores;`,
    choices: [
      'LAG/LEAD 함수가 현재 행의 이전/다음 행 값을 참조해야 하므로, 파티션 내 행들을 메모리 버퍼에 보관하여 인접 행 접근을 가능하게 하는 단계',
      'TABLE ACCESS FULL의 I/O 비용을 줄이기 위해 읽어온 블록을 버퍼 캐시에 저장하는 Oracle SGA 관련 단계',
      'PARTITION BY user_id 기준으로 데이터를 나누어 각 파티션을 별도의 임시 테이블로 저장하는 단계',
      'scores 테이블이 파티션 테이블일 때 해당 파티션 데이터를 메모리에 로드하는 파티션 프루닝 단계',
    ],
    choiceExplanations: [
      { value: 'LAG/LEAD 함수가 현재 행의 이전/다음 행 값을 참조해야 하므로, 파티션 내 행들을 메모리 버퍼에 보관하여 인접 행 접근을 가능하게 하는 단계', explanation: '정답입니다. WINDOW BUFFER는 LAG/LEAD처럼 인접 행을 참조하는 윈도우 함수를 위해 파티션 내 행들을 버퍼에 보관합니다.' },
      { value: 'TABLE ACCESS FULL의 I/O 비용을 줄이기 위해 읽어온 블록을 버퍼 캐시에 저장하는 Oracle SGA 관련 단계', explanation: '오답입니다. 실행 계획의 WINDOW BUFFER는 Oracle SGA 버퍼 캐시와 무관하며, 윈도우 함수 처리를 위한 쿼리 실행 단계입니다.' },
      { value: 'PARTITION BY user_id 기준으로 데이터를 나누어 각 파티션을 별도의 임시 테이블로 저장하는 단계', explanation: '오답입니다. WINDOW BUFFER는 임시 테이블을 생성하지 않습니다. 인접 행 접근을 위해 메모리 내에서 행을 보관하는 단계입니다.' },
      { value: 'scores 테이블이 파티션 테이블일 때 해당 파티션 데이터를 메모리에 로드하는 파티션 프루닝 단계', explanation: '오답입니다. 파티션 프루닝은 PARTITION RANGE ALL 등으로 표시됩니다. WINDOW BUFFER는 테이블 파티셔닝과 관계없이 LAG/LEAD 처리를 위한 단계입니다.' },
    ],
    correctAnswer: 'LAG/LEAD 함수가 현재 행의 이전/다음 행 값을 참조해야 하므로, 파티션 내 행들을 메모리 버퍼에 보관하여 인접 행 접근을 가능하게 하는 단계',
    hints: {
      directional: ['LAG 함수가 처리되기 위해 어떤 행 정보가 필요한지 생각해보세요'],
      constraint: ['WINDOW BUFFER는 LAG/LEAD 전용 실행 단계입니다'],
      misconception: ['Oracle 버퍼 캐시와 실행 계획의 WINDOW BUFFER는 서로 다른 개념입니다'],
    },
  } as PlanProblem,
  {
    id: 'swp03',
    category: 'sql-window',
    difficulty: 'intermediate',
    type: 'plan',
    availableModes: ['write', 'fill'] as const,
    title: '광고 캠페인 Top-N 최적화 실행 계획 (WINDOW SORT PUSHED RANK)',
    question: '아래 Oracle 실행 계획은 광고 캠페인에서 카테고리별 상위 3개 광고를 추출하는 쿼리의 실행 계획입니다. "WINDOW SORT PUSHED RANK" 단계의 최적화 의미는 무엇입니까?',
    learningPoint: 'WINDOW SORT PUSHED RANK는 Top-N 쿼리에서 Oracle이 전체 정렬 없이 조기에 N건 이상을 자르는 최적화를 적용한 실행 단계이다',
    tags: ['WINDOW SORT PUSHED RANK', 'ROW_NUMBER', 'TOP-N', '최적화'],
    explanation: 'WINDOW SORT PUSHED RANK는 ROW_NUMBER/RANK와 WHERE rn <= N 패턴에서 Oracle 옵티마이저가 적용하는 최적화입니다. 전체 데이터를 정렬하지 않고, 각 파티션에서 N건만 유지하면서 처리하여 메모리와 CPU를 절약합니다.',
    relatedConceptTags: ['window-function', 'execution-plan', 'optimization'],
    planText: `------------------------------------------------------------------
| Id | Operation                    | Name    | Rows | Cost (%CPU)|
------------------------------------------------------------------
|  0 | SELECT STATEMENT             |         |   30 |    13   (8)|
|* 1 |  VIEW                        |         |   30 |    13   (8)|
|* 2 |   WINDOW SORT PUSHED RANK    |         |  300 |    13   (8)|
|  3 |    TABLE ACCESS FULL         | AD_LOGS |  300 |    12   (0)|
------------------------------------------------------------------
Predicate Information:
   1 - filter("RN"<=3)
   2 - filter(RANK() OVER (PARTITION BY "CATEGORY" ORDER BY "CTR" DESC)<=3)

Query:
SELECT * FROM (
  SELECT ad_id, category, ctr,
    ROW_NUMBER() OVER (PARTITION BY category ORDER BY ctr DESC) AS rn
  FROM ad_logs
) WHERE rn <= 3;`,
    choices: [
      '각 파티션에서 상위 N건 이상 데이터를 조기에 제거하여 전체 정렬 비용을 줄이는 Top-N 최적화 단계로, 불필요한 행을 소트 중에 미리 필터링한다',
      '윈도우 함수 계산 전 WHERE 조건을 먼저 적용하는 Predicate Pushdown으로, 테이블에서 읽는 행 수 자체를 줄이는 단계',
      'RANK 함수를 DENSE_RANK로 자동 변환하여 순위 계산 성능을 향상시키는 옵티마이저 변환 단계',
      '인덱스 스캔이 불가할 때 소트-머지 조인 방식으로 데이터를 처리하는 대체 실행 경로',
    ],
    choiceExplanations: [
      { value: '각 파티션에서 상위 N건 이상 데이터를 조기에 제거하여 전체 정렬 비용을 줄이는 Top-N 최적화 단계로, 불필요한 행을 소트 중에 미리 필터링한다', explanation: '정답입니다. WINDOW SORT PUSHED RANK는 Top-N 쿼리 패턴(rn <= N)에서 Oracle이 소트 과정 중 N건을 초과하는 행을 조기에 버려 메모리와 CPU를 절약하는 최적화입니다.' },
      { value: '윈도우 함수 계산 전 WHERE 조건을 먼저 적용하는 Predicate Pushdown으로, 테이블에서 읽는 행 수 자체를 줄이는 단계', explanation: '오답입니다. 이 최적화는 테이블에서 읽는 행 수를 직접 줄이지 않습니다. WINDOW SORT PUSHED RANK는 소트 단계에서 불필요 행을 제거하는 최적화입니다.' },
      { value: 'RANK 함수를 DENSE_RANK로 자동 변환하여 순위 계산 성능을 향상시키는 옵티마이저 변환 단계', explanation: '오답입니다. Oracle 옵티마이저는 RANK를 DENSE_RANK로 변환하지 않습니다. WINDOW SORT PUSHED RANK는 Top-N 패턴 감지 시 적용되는 소트 최적화입니다.' },
      { value: '인덱스 스캔이 불가할 때 소트-머지 조인 방식으로 데이터를 처리하는 대체 실행 경로', explanation: '오답입니다. 소트-머지 조인(SORT MERGE JOIN)은 두 테이블을 조인할 때 사용됩니다. WINDOW SORT PUSHED RANK는 단일 테이블 윈도우 함수 처리의 Top-N 최적화 단계입니다.' },
    ],
    correctAnswer: '각 파티션에서 상위 N건 이상 데이터를 조기에 제거하여 전체 정렬 비용을 줄이는 Top-N 최적화 단계로, 불필요한 행을 소트 중에 미리 필터링한다',
    hints: {
      directional: ['WHERE rn <= 3 조건이 실행 계획에서 어떻게 소트 단계에 영향을 주는지 생각해보세요'],
      constraint: ['PUSHED RANK의 "PUSHED"는 조건이 소트 단계로 밀려들어간다는 의미입니다'],
      misconception: ['이 단계는 테이블에서 읽는 행 수를 줄이는 것이 아니라 소트 중 불필요 행을 버리는 최적화입니다'],
    },
  } as PlanProblem,
  {
    id: 'swp04',
    category: 'sql-window',
    difficulty: 'intermediate',
    type: 'plan',
    availableModes: ['write', 'fill'] as const,
    title: '배달 주문 누적 집계 후 정렬 실행 계획 (WINDOW SORT + SORT ORDER BY)',
    question: '아래 Oracle 실행 계획에는 WINDOW SORT와 SORT ORDER BY가 모두 나타납니다. 이 두 단계가 각각 별도로 존재하는 이유는 무엇입니까?',
    learningPoint: 'WINDOW SORT의 ORDER BY와 최종 출력의 ORDER BY가 다를 때 두 개의 소트 단계가 발생한다',
    tags: ['WINDOW SORT', 'SORT ORDER BY', 'ORDER BY', '실행계획'],
    explanation: 'WINDOW SORT는 OVER(PARTITION BY restaurant_id ORDER BY delivered_at) 처리를 위해 restaurant_id, delivered_at 기준으로 정렬합니다. 이후 최종 출력을 cumulative_amount DESC로 정렬해야 하므로 별도의 SORT ORDER BY 단계가 추가됩니다. 두 정렬 기준이 다르기 때문에 두 번 정렬이 발생합니다.',
    relatedConceptTags: ['window-function', 'execution-plan'],
    planText: `--------------------------------------------------------------------
| Id | Operation           | Name       | Rows | Cost (%CPU)|
--------------------------------------------------------------------
|  0 | SELECT STATEMENT    |            | 2000 |    28  (15)|
|  1 |  SORT ORDER BY      |            | 2000 |    28  (15)|
|  2 |   WINDOW SORT       |            | 2000 |    25   (8)|
|  3 |    TABLE ACCESS FULL| DELIVERIES | 2000 |    24   (0)|
--------------------------------------------------------------------

Query:
SELECT order_id, restaurant_id, amount, delivered_at,
  SUM(amount) OVER (PARTITION BY restaurant_id ORDER BY delivered_at) AS cumulative_amount
FROM deliveries
ORDER BY cumulative_amount DESC;`,
    choices: [
      'WINDOW SORT는 OVER 절의 PARTITION BY restaurant_id ORDER BY delivered_at을 위한 정렬이고, SORT ORDER BY는 최종 출력의 ORDER BY cumulative_amount DESC를 위한 별도 정렬이다',
      'WINDOW SORT는 데이터를 읽는 단계이고 SORT ORDER BY는 중복을 제거하는 단계로, 두 단계가 합쳐져 DISTINCT와 동일한 효과를 낸다',
      'Oracle이 비용 절감을 위해 한 번의 소트를 두 단계로 나누어 메모리 사용량을 줄이는 분할 정복 방식의 실행 계획이다',
      'WINDOW SORT는 파티션 키(restaurant_id) 기준 해시 분배 후 정렬이고, SORT ORDER BY는 병렬 처리 결과를 하나로 합치는 병합 단계이다',
    ],
    choiceExplanations: [
      { value: 'WINDOW SORT는 OVER 절의 PARTITION BY restaurant_id ORDER BY delivered_at을 위한 정렬이고, SORT ORDER BY는 최종 출력의 ORDER BY cumulative_amount DESC를 위한 별도 정렬이다', explanation: '정답입니다. 두 ORDER BY의 기준이 다르기 때문에 Oracle은 윈도우 함수 처리용 정렬과 최종 출력 정렬을 각각 별도로 수행합니다.' },
      { value: 'WINDOW SORT는 데이터를 읽는 단계이고 SORT ORDER BY는 중복을 제거하는 단계로, 두 단계가 합쳐져 DISTINCT와 동일한 효과를 낸다', explanation: '오답입니다. WINDOW SORT는 데이터를 읽는 단계가 아닙니다. 두 소트 단계 모두 중복 제거와 무관하며, 각각 다른 정렬 목적을 가집니다.' },
      { value: 'Oracle이 비용 절감을 위해 한 번의 소트를 두 단계로 나누어 메모리 사용량을 줄이는 분할 정복 방식의 실행 계획이다', explanation: '오답입니다. 두 소트 단계는 비용 절감을 위한 분할이 아닙니다. 윈도우 함수용 정렬과 최종 출력 정렬의 기준이 달라 불가피하게 두 번 정렬이 발생합니다.' },
      { value: 'WINDOW SORT는 파티션 키(restaurant_id) 기준 해시 분배 후 정렬이고, SORT ORDER BY는 병렬 처리 결과를 하나로 합치는 병합 단계이다', explanation: '오답입니다. 이 실행 계획은 병렬 처리(PARALLEL)를 사용하지 않습니다. 두 소트 단계는 단순히 서로 다른 ORDER BY 기준을 처리하기 위한 순차적 단계입니다.' },
    ],
    correctAnswer: 'WINDOW SORT는 OVER 절의 PARTITION BY restaurant_id ORDER BY delivered_at을 위한 정렬이고, SORT ORDER BY는 최종 출력의 ORDER BY cumulative_amount DESC를 위한 별도 정렬이다',
    hints: {
      directional: ['쿼리에서 ORDER BY가 몇 번 사용되었는지, 각각 어떤 기준인지 확인해보세요'],
      constraint: ['WINDOW SORT의 정렬 기준(delivered_at)과 SORT ORDER BY의 기준(cumulative_amount DESC)이 다릅니다'],
      misconception: ['WINDOW SORT와 SORT ORDER BY는 같은 정렬을 중복 수행하는 것이 아닙니다. 각각 목적이 다른 별도의 정렬 단계입니다'],
    },
  } as PlanProblem,
  {
    id: 'swp05',
    category: 'sql-window',
    difficulty: 'advanced',
    type: 'plan',
    availableModes: ['write', 'fill'] as const,
    title: '구독 플랜별 집계와 윈도우 함수 결합 실행 계획 (HASH GROUP BY + WINDOW SORT)',
    question: '아래 Oracle 실행 계획은 구독 플랜별 총 매출을 집계하고 전월 대비 변화량을 계산하는 쿼리의 실행 계획입니다. HASH GROUP BY와 WINDOW SORT가 함께 나타나는 이유와 처리 순서를 올바르게 설명한 것은 무엇입니까?',
    learningPoint: 'HASH GROUP BY로 먼저 집계한 후 WINDOW SORT로 윈도우 함수를 처리하는 순서로 실행된다. 집계 결과에 윈도우 함수를 적용하는 패턴이다',
    tags: ['HASH GROUP BY', 'WINDOW SORT', 'LAG', 'GROUP BY', '집계'],
    explanation: 'GROUP BY를 포함한 쿼리에 윈도우 함수를 적용할 때, Oracle은 먼저 HASH GROUP BY로 집계를 완료한 후 그 결과에 WINDOW SORT를 적용하여 윈도우 함수를 계산합니다. 이는 SQL의 논리적 처리 순서(GROUP BY → 윈도우 함수)를 반영합니다.',
    relatedConceptTags: ['window-function', 'execution-plan', 'group-by'],
    planText: `--------------------------------------------------------------------
| Id | Operation            | Name          | Rows | Cost (%CPU)|
--------------------------------------------------------------------
|  0 | SELECT STATEMENT     |               |   12 |    18  (17)|
|  1 |  WINDOW SORT         |               |   12 |    18  (17)|
|  2 |   HASH GROUP BY      |               |   12 |    18  (17)|
|  3 |    TABLE ACCESS FULL | SUBSCRIPTIONS | 5000 |    15   (0)|
--------------------------------------------------------------------

Query:
SELECT plan_id,
       TO_CHAR(TRUNC(start_date, 'MM'), 'YYYY-MM') AS month,
       SUM(amount) AS monthly_revenue,
       LAG(SUM(amount)) OVER (PARTITION BY plan_id ORDER BY TRUNC(start_date, 'MM')) AS prev_revenue
FROM subscriptions
GROUP BY plan_id, TRUNC(start_date, 'MM');`,
    choices: [
      'TABLE ACCESS FULL → HASH GROUP BY(plan_id, 월별 집계) → WINDOW SORT(집계 결과에 LAG 적용) 순으로 실행되며, GROUP BY 집계가 완료된 후 윈도우 함수가 그 결과에 적용된다',
      'TABLE ACCESS FULL → WINDOW SORT(LAG 계산) → HASH GROUP BY(plan_id, 월별 집계) 순으로 실행되며, 윈도우 함수가 먼저 처리된 후 GROUP BY로 집계된다',
      'HASH GROUP BY와 WINDOW SORT는 병렬로 동시에 실행되어 TABLE ACCESS FULL 결과를 각각 독립적으로 처리한 뒤 최종 결합한다',
      'HASH GROUP BY는 GROUP BY 없이 DISTINCT 처리를 위해, WINDOW SORT는 ORDER BY 출력 정렬을 위해 각각 사용되는 독립적 단계이다',
    ],
    choiceExplanations: [
      { value: 'TABLE ACCESS FULL → HASH GROUP BY(plan_id, 월별 집계) → WINDOW SORT(집계 결과에 LAG 적용) 순으로 실행되며, GROUP BY 집계가 완료된 후 윈도우 함수가 그 결과에 적용된다', explanation: '정답입니다. Oracle 실행 계획은 아래에서 위로 읽습니다. FULL SCAN → HASH GROUP BY → WINDOW SORT 순으로 처리되며, GROUP BY 집계 결과에 LAG 윈도우 함수가 적용됩니다.' },
      { value: 'TABLE ACCESS FULL → WINDOW SORT(LAG 계산) → HASH GROUP BY(plan_id, 월별 집계) 순으로 실행되며, 윈도우 함수가 먼저 처리된 후 GROUP BY로 집계된다', explanation: '오답입니다. 실행 계획은 아래에서 위로 읽으므로 HASH GROUP BY(Id=2)가 WINDOW SORT(Id=1)보다 먼저 실행됩니다. SQL 논리적 순서상 GROUP BY가 윈도우 함수보다 먼저 처리됩니다.' },
      { value: 'HASH GROUP BY와 WINDOW SORT는 병렬로 동시에 실행되어 TABLE ACCESS FULL 결과를 각각 독립적으로 처리한 뒤 최종 결합한다', explanation: '오답입니다. 이 실행 계획은 병렬 실행이 아닙니다. HASH GROUP BY와 WINDOW SORT는 순차적으로 실행되며, WINDOW SORT는 HASH GROUP BY의 출력을 입력으로 받습니다.' },
      { value: 'HASH GROUP BY는 GROUP BY 없이 DISTINCT 처리를 위해, WINDOW SORT는 ORDER BY 출력 정렬을 위해 각각 사용되는 독립적 단계이다', explanation: '오답입니다. 이 쿼리에는 GROUP BY가 명시되어 있으므로 HASH GROUP BY는 GROUP BY 집계를 위한 단계입니다. WINDOW SORT는 출력 정렬이 아닌 LAG 윈도우 함수 처리를 위한 단계입니다.' },
    ],
    correctAnswer: 'TABLE ACCESS FULL → HASH GROUP BY(plan_id, 월별 집계) → WINDOW SORT(집계 결과에 LAG 적용) 순으로 실행되며, GROUP BY 집계가 완료된 후 윈도우 함수가 그 결과에 적용된다',
    hints: {
      directional: ['Oracle 실행 계획은 Id가 높은 단계(아래)부터 실행됩니다. Id 순서대로 처리 흐름을 따라가 보세요'],
      constraint: ['SQL 논리적 처리 순서에서 GROUP BY는 윈도우 함수보다 먼저 처리됩니다'],
      misconception: ['실행 계획을 위에서 아래로 읽으면 실행 순서가 반대로 이해될 수 있습니다. 실제 실행은 Id가 큰 단계부터 시작합니다'],
    },
  } as PlanProblem,
  // ── swf17 ~ swf30 ──────────────────────────────────────────────
  {
    id: 'swf17',
    category: 'sql-window',
    difficulty: 'basic',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '사용자별 주문 금액 누적 합계 구하기',
    question: '이커머스 주문 테이블에서 사용자별로 주문 날짜 순서대로 누적 금액을 구하는 SQL의 빈칸을 채우세요.',
    learningPoint: 'SUM() OVER (PARTITION BY ... ORDER BY ...)로 누적 합계를 계산한다',
    tags: ['SUM', 'OVER', 'PARTITION BY', 'ORDER BY'],
    explanation: 'SUM(amount) OVER (PARTITION BY user_id ORDER BY created_at)는 user_id별로 created_at 순서에 따라 누적 합계를 계산합니다. GROUP BY와 달리 원래 행이 유지됩니다.',
    relatedConceptTags: ['window-function'],
    sqlTemplate: 'SELECT order_id, user_id, amount,\n  ___(amount) OVER (PARTITION BY user_id ___ created_at) AS running_total\nFROM orders;',
    blanks: 2,
    options: ['SUM', 'AVG', 'COUNT', 'MAX', 'ORDER BY', 'PARTITION BY', 'GROUP BY'],
    optionExplanations: [
      { value: 'SUM', explanation: '누적 합계를 구하는 집계 함수입니다. OVER와 함께 윈도우 함수로 동작합니다' },
      { value: 'AVG', explanation: '평균을 구합니다. 누적 합계에는 적합하지 않습니다' },
      { value: 'COUNT', explanation: '행 개수를 셉니다. 금액 합계에는 맞지 않습니다' },
      { value: 'MAX', explanation: '최대값을 반환합니다. 누적 합계와는 다릅니다' },
      { value: 'ORDER BY', explanation: '윈도우 내에서 정렬 순서를 지정합니다. ORDER BY가 있으면 기본 프레임이 누적 합계로 동작합니다' },
      { value: 'PARTITION BY', explanation: '윈도우를 그룹으로 나누는 키워드입니다. 이미 앞에 있으므로 이 위치는 정렬 키워드입니다' },
      { value: 'GROUP BY', explanation: 'GROUP BY는 행을 묶어 집계합니다. OVER 절 내에서는 사용할 수 없습니다' },
    ],
    correctAnswers: ['SUM', 'ORDER BY'],
    hints: {
      directional: ['누적 합계를 구하는 집계 함수를 선택하세요'],
      constraint: ['PARTITION BY user_id 다음에 날짜 정렬을 위한 키워드가 필요합니다'],
      misconception: ['GROUP BY를 사용하면 원래 행이 줄어듭니다. OVER를 사용하면 행 수가 유지됩니다'],
    },
  } as FillProblem,
  {
    id: 'swf18',
    category: 'sql-window',
    difficulty: 'basic',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '게임 점수 이전 기록과 현재 기록 비교하기',
    question: '게임 점수 테이블에서 각 사용자의 이전 게임 점수를 함께 조회하는 SQL의 빈칸을 채우세요.',
    learningPoint: 'LAG()는 이전 행의 값을 가져오는 윈도우 함수다',
    tags: ['LAG', 'OVER', 'PARTITION BY'],
    explanation: 'LAG(score, 1)은 현재 행 기준 1행 앞의 score 값을 반환합니다. 첫 번째 행은 이전 값이 없으므로 NULL이 반환됩니다.',
    relatedConceptTags: ['window-function'],
    sqlTemplate: 'SELECT user_id, score, played_at,\n  ___(score, 1) OVER (PARTITION BY user_id ORDER BY ___) AS prev_score\nFROM scores;',
    blanks: 2,
    options: ['LAG', 'LEAD', 'FIRST_VALUE', 'LAST_VALUE', 'played_at', 'user_id', 'score'],
    optionExplanations: [
      { value: 'LAG', explanation: '이전 행의 값을 가져옵니다. LAG(col, n)으로 n행 앞 값을 가져올 수 있습니다' },
      { value: 'LEAD', explanation: '다음 행의 값을 가져옵니다. 이전 행이 아닌 미래 행 조회에 사용합니다' },
      { value: 'FIRST_VALUE', explanation: '윈도우 내 첫 번째 값을 반환합니다. 이전 행 값과는 다릅니다' },
      { value: 'LAST_VALUE', explanation: '윈도우 내 마지막 값을 반환합니다. 이전 행 값이 아닙니다' },
      { value: 'played_at', explanation: '게임 플레이 시각으로, 시간 순서 정렬에 적합한 컬럼입니다' },
      { value: 'user_id', explanation: '사용자 ID로, 이미 PARTITION BY에 사용되었습니다' },
      { value: 'score', explanation: '점수 컬럼입니다. 시간 순서 정렬 기준으로는 적합하지 않습니다' },
    ],
    correctAnswers: ['LAG', 'played_at'],
    hints: {
      directional: ['이전 행의 값을 가져오는 함수를 선택하세요'],
      constraint: ['시간 순서로 정렬하려면 날짜/시각 컬럼을 ORDER BY해야 합니다'],
      misconception: ['LEAD는 다음 행, LAG는 이전 행입니다. 방향을 헷갈리지 마세요'],
    },
  } as FillProblem,
  {
    id: 'swf19',
    category: 'sql-window',
    difficulty: 'basic',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '광고 이벤트 광고별 발생 순번 매기기',
    question: '광고 로그 테이블에서 광고별로 이벤트 발생 시각 순서대로 고유한 순번을 매기는 SQL의 빈칸을 채우세요.',
    learningPoint: 'ROW_NUMBER()는 파티션 내에서 중복 없는 고유 순번을 부여한다',
    tags: ['ROW_NUMBER', 'PARTITION BY', 'ORDER BY'],
    explanation: 'ROW_NUMBER() OVER (PARTITION BY ad_id ORDER BY created_at)는 ad_id별로 created_at 오름차순으로 1부터 시작하는 고유 번호를 부여합니다.',
    relatedConceptTags: ['window-function'],
    sqlTemplate: 'SELECT ad_id, user_id, event_type,\n  ___() OVER (PARTITION BY ad_id ORDER BY ___) AS event_seq\nFROM ad_logs;',
    blanks: 2,
    options: ['ROW_NUMBER', 'RANK', 'DENSE_RANK', 'NTILE', 'created_at', 'ad_id', 'user_id', 'event_type'],
    optionExplanations: [
      { value: 'ROW_NUMBER', explanation: '파티션 내에서 항상 고유한 순번(1,2,3...)을 부여합니다' },
      { value: 'RANK', explanation: '동일 값이면 같은 순위를 부여합니다. 순번이 고유하지 않을 수 있습니다' },
      { value: 'DENSE_RANK', explanation: '동일 값에 같은 순위를 주되 건너뜀 없이 연속합니다. 역시 고유 순번이 아닙니다' },
      { value: 'NTILE', explanation: '파티션을 N개 그룹으로 나눕니다. 순번 부여와는 다릅니다' },
      { value: 'created_at', explanation: '이벤트 발생 시각으로, 시간 순서 정렬에 적합한 컬럼입니다' },
      { value: 'ad_id', explanation: '광고 ID로, PARTITION BY에 이미 사용되었습니다' },
      { value: 'user_id', explanation: '사용자 ID입니다. 이벤트 순번의 시간 정렬 기준이 되는 컬럼이 아닙니다' },
      { value: 'event_type', explanation: '이벤트 유형입니다. 시간 순서 정렬 기준으로는 맞지 않습니다' },
    ],
    correctAnswers: ['ROW_NUMBER', 'created_at'],
    hints: {
      directional: ['중복 없는 고유 순번을 부여하는 함수를 선택하세요'],
      constraint: ['시간 순서대로 정렬하려면 어떤 컬럼을 ORDER BY해야 할까요?'],
      misconception: ['RANK는 동일 값에 같은 순위를 줄 수 있어 고유 순번이 보장되지 않습니다'],
    },
  } as FillProblem,
  {
    id: 'swf20',
    category: 'sql-window',
    difficulty: 'basic',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '구독 플랜별 평균 결제 금액과 각 행 비교하기',
    question: '구독 테이블에서 각 사용자의 금액과 해당 플랜 평균 금액을 함께 조회하는 SQL의 빈칸을 채우세요.',
    learningPoint: 'AVG() OVER (PARTITION BY ...)로 그룹별 평균을 원본 행과 함께 조회한다',
    tags: ['AVG', 'OVER', 'PARTITION BY'],
    explanation: 'AVG(amount) OVER (PARTITION BY plan_id)는 plan_id별 평균 금액을 각 행에 함께 표시합니다. GROUP BY와 달리 원래 행이 유지됩니다.',
    relatedConceptTags: ['window-function'],
    sqlTemplate: 'SELECT user_id, plan_id, amount,\n  ___(amount) OVER (PARTITION BY ___) AS plan_avg\nFROM subscriptions;',
    blanks: 2,
    options: ['AVG', 'SUM', 'MAX', 'MIN', 'plan_id', 'user_id', 'amount', 'start_date'],
    optionExplanations: [
      { value: 'AVG', explanation: '평균값을 구하는 집계 함수입니다. OVER와 함께 윈도우 함수로도 사용됩니다' },
      { value: 'SUM', explanation: '합계를 구합니다. 평균이 아닌 합계가 됩니다' },
      { value: 'MAX', explanation: '최대값을 반환합니다. 평균과는 다른 결과입니다' },
      { value: 'MIN', explanation: '최소값을 반환합니다. 평균과는 다릅니다' },
      { value: 'plan_id', explanation: '플랜 ID로, 플랜별 그룹을 나누는 PARTITION BY 기준입니다' },
      { value: 'user_id', explanation: '사용자 ID입니다. 사용자별 평균이면 맞지만, 플랜별 평균과는 다릅니다' },
      { value: 'amount', explanation: '금액 컬럼으로 집계 대상이지 그룹 기준이 아닙니다' },
      { value: 'start_date', explanation: '시작일은 그룹 분할 기준으로 적합하지 않습니다' },
    ],
    correctAnswers: ['AVG', 'plan_id'],
    hints: {
      directional: ['평균을 계산하는 집계 함수를 선택하세요'],
      constraint: ['플랜별 평균을 구하려면 PARTITION BY 뒤에 어떤 컬럼이 와야 할까요?'],
      misconception: ['SUM은 합계, AVG는 평균입니다. 목표에 맞는 함수를 선택하세요'],
    },
  } as FillProblem,
  {
    id: 'swf21',
    category: 'sql-window',
    difficulty: 'basic',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '배달 레스토랑별 다음 주문 배달 소요 시간 미리 보기',
    question: '배달 테이블에서 레스토랑별로 배달 완료 시각 순서로 다음 주문의 배달 소요 시간을 함께 조회하는 SQL의 빈칸을 채우세요.',
    learningPoint: 'LEAD()는 다음 행의 값을 가져오는 윈도우 함수다',
    tags: ['LEAD', 'OVER', 'PARTITION BY'],
    explanation: 'LEAD(delivery_time_min, 1)은 현재 행 기준 1행 뒤의 delivery_time_min 값을 반환합니다. 마지막 행은 다음 값이 없어 NULL이 반환됩니다.',
    relatedConceptTags: ['window-function'],
    sqlTemplate: 'SELECT order_id, restaurant_id, delivery_time_min,\n  ___(delivery_time_min, 1) OVER (PARTITION BY restaurant_id ORDER BY ___) AS next_delivery_time\nFROM deliveries;',
    blanks: 2,
    options: ['LEAD', 'LAG', 'LAST_VALUE', 'FIRST_VALUE', 'delivered_at', 'restaurant_id', 'order_id', 'rating'],
    optionExplanations: [
      { value: 'LEAD', explanation: '다음 행의 값을 가져옵니다. LEAD(col, n)으로 n행 뒤 값을 가져올 수 있습니다' },
      { value: 'LAG', explanation: '이전 행의 값을 가져옵니다. 다음 행이 아닌 이전 행 조회에 사용합니다' },
      { value: 'LAST_VALUE', explanation: '윈도우 내 마지막 값을 반환합니다. 바로 다음 행 값과는 다릅니다' },
      { value: 'FIRST_VALUE', explanation: '윈도우 내 첫 번째 값을 반환합니다. 다음 행 값이 아닙니다' },
      { value: 'delivered_at', explanation: '배달 완료 시각으로, 시간 순서 정렬에 적합합니다' },
      { value: 'restaurant_id', explanation: '레스토랑 ID로, 이미 PARTITION BY에 사용되었습니다' },
      { value: 'order_id', explanation: '주문 ID는 시간 순서 정렬에 적합하지 않을 수 있습니다' },
      { value: 'rating', explanation: '평점은 시간 순서 정렬 기준으로 맞지 않습니다' },
    ],
    correctAnswers: ['LEAD', 'delivered_at'],
    hints: {
      directional: ['다음 행의 값을 가져오는 함수를 선택하세요'],
      constraint: ['시간 순서로 정렬하려면 날짜/시각 컬럼을 사용해야 합니다'],
      misconception: ['LAG는 이전 행, LEAD는 다음 행입니다. 방향을 확인하세요'],
    },
  } as FillProblem,
  {
    id: 'swf22',
    category: 'sql-window',
    difficulty: 'intermediate',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '스트리밍 콘텐츠 완료 시청 수 기준 순위 매기기',
    question: '시청 로그 테이블에서 콘텐츠별로 완료 시청 수(completed=true) 순위를 DENSE_RANK로 매기는 SQL의 빈칸을 채우세요.',
    learningPoint: 'DENSE_RANK()는 동일 값에 같은 순위를 주고, 다음 순위를 건너뛰지 않는다',
    tags: ['DENSE_RANK', 'OVER', 'ORDER BY'],
    explanation: 'DENSE_RANK()는 동일한 값에 같은 순위를 부여하되 1,1,2,3처럼 순위를 건너뛰지 않습니다. RANK()는 1,1,3처럼 건너뜁니다.',
    relatedConceptTags: ['window-function'],
    sqlTemplate: 'SELECT content_id,\n  COUNT(*) FILTER (WHERE completed = true) AS complete_count,\n  ___() OVER (ORDER BY COUNT(*) FILTER (WHERE completed = true) ___) AS content_rank\nFROM watch_logs\nGROUP BY content_id;',
    blanks: 2,
    options: ['DENSE_RANK', 'RANK', 'ROW_NUMBER', 'NTILE', 'DESC', 'ASC', 'NULLS LAST', 'NULLS FIRST'],
    optionExplanations: [
      { value: 'DENSE_RANK', explanation: '동일 값에 같은 순위를 주되 건너뜀 없이 연속 순위를 부여합니다 (1,1,2,3)' },
      { value: 'RANK', explanation: '동일 값에 같은 순위를 주지만 다음 순위를 건너뜁니다 (1,1,3)' },
      { value: 'ROW_NUMBER', explanation: '항상 고유한 순번을 부여합니다. 동일 값이어도 다른 번호입니다' },
      { value: 'NTILE', explanation: 'N개의 그룹으로 나눕니다. 순위 함수가 아닙니다' },
      { value: 'DESC', explanation: '내림차순 정렬입니다. 완료 시청 수가 많을수록 낮은 순위 번호(1위)를 받습니다' },
      { value: 'ASC', explanation: '오름차순 정렬입니다. 완료 수가 적을수록 낮은 순위 번호를 받게 됩니다' },
      { value: 'NULLS LAST', explanation: 'NULL 값을 마지막에 배치하는 옵션입니다. 정렬 방향 자체가 아닙니다' },
      { value: 'NULLS FIRST', explanation: 'NULL 값을 처음에 배치하는 옵션입니다. 정렬 방향이 아닙니다' },
    ],
    correctAnswers: ['DENSE_RANK', 'DESC'],
    hints: {
      directional: ['건너뜀 없는 연속 순위를 부여하는 함수를 선택하세요'],
      constraint: ['완료 시청 수가 많을수록 1위가 되려면 어떤 정렬 방향이 필요한가요?'],
      misconception: ['RANK는 (1,1,3), DENSE_RANK는 (1,1,2)처럼 동작합니다'],
    },
  } as FillProblem,
  {
    id: 'swf23',
    category: 'sql-window',
    difficulty: 'intermediate',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '이커머스 사용자별 첫 번째 주문 금액 모든 행에 표시',
    question: '주문 테이블에서 각 사용자의 모든 주문 행에 첫 번째 주문 금액을 함께 표시하는 SQL의 빈칸을 채우세요.',
    learningPoint: 'FIRST_VALUE()는 파티션 내 첫 번째 행의 값을 반환한다',
    tags: ['FIRST_VALUE', 'OVER', 'PARTITION BY', 'ORDER BY'],
    explanation: 'FIRST_VALUE(amount) OVER (PARTITION BY user_id ORDER BY created_at ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)는 user_id별로 가장 빠른 주문의 amount를 모든 행에 표시합니다.',
    relatedConceptTags: ['window-function'],
    sqlTemplate: 'SELECT order_id, user_id, amount, created_at,\n  ___(amount) OVER (PARTITION BY user_id ORDER BY created_at\n    ROWS BETWEEN ___ AND CURRENT ROW) AS first_order_amount\nFROM orders;',
    blanks: 2,
    options: ['FIRST_VALUE', 'LAST_VALUE', 'LAG', 'LEAD', 'UNBOUNDED PRECEDING', 'CURRENT ROW', '1 PRECEDING', 'UNBOUNDED FOLLOWING'],
    optionExplanations: [
      { value: 'FIRST_VALUE', explanation: '파티션 내 첫 번째 행의 값을 반환합니다' },
      { value: 'LAST_VALUE', explanation: '파티션 내 마지막 행의 값을 반환합니다. 첫 번째 값이 아닙니다' },
      { value: 'LAG', explanation: '이전 행의 값을 가져옵니다. 첫 번째 행 값과는 다릅니다' },
      { value: 'LEAD', explanation: '다음 행의 값을 가져옵니다. 첫 번째 행 값이 아닙니다' },
      { value: 'UNBOUNDED PRECEDING', explanation: '파티션의 시작부터라는 의미입니다. FIRST_VALUE와 함께 쓰면 항상 첫 값을 보장합니다' },
      { value: 'CURRENT ROW', explanation: '현재 행을 나타냅니다. 이미 AND 뒤에 있습니다' },
      { value: '1 PRECEDING', explanation: '1행 앞을 의미합니다. FIRST_VALUE의 올바른 프레임이 아닙니다' },
      { value: 'UNBOUNDED FOLLOWING', explanation: '파티션의 끝까지를 의미합니다. 첫 값을 구할 때는 필요하지 않습니다' },
    ],
    correctAnswers: ['FIRST_VALUE', 'UNBOUNDED PRECEDING'],
    hints: {
      directional: ['파티션 내 첫 번째 값을 반환하는 함수를 선택하세요'],
      constraint: ['ROWS BETWEEN ___ AND CURRENT ROW에서 파티션 시작부터를 나타내는 표현은 무엇인가요?'],
      misconception: ['FIRST_VALUE는 ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW 프레임이 권장됩니다'],
    },
  } as FillProblem,
  {
    id: 'swf24',
    category: 'sql-window',
    difficulty: 'intermediate',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '게임 캐릭터 경험치 기준 4개 등급으로 분류하기',
    question: '캐릭터 테이블에서 경험치(exp) 순서로 캐릭터를 4개 등급으로 분류하는 SQL의 빈칸을 채우세요.',
    learningPoint: 'NTILE(n)은 파티션을 n개의 동일한 크기 그룹으로 나눈다',
    tags: ['NTILE', 'OVER', 'ORDER BY'],
    explanation: 'NTILE(4) OVER (ORDER BY exp DESC)는 모든 캐릭터를 경험치 내림차순으로 4개 그룹(1~4)으로 나눕니다. 그룹 1이 상위 그룹입니다.',
    relatedConceptTags: ['window-function'],
    sqlTemplate: 'SELECT user_id, level, exp,\n  ___(4) OVER (ORDER BY exp ___) AS exp_tier\nFROM characters;',
    blanks: 2,
    options: ['NTILE', 'ROW_NUMBER', 'RANK', 'DENSE_RANK', 'DESC', 'ASC', 'NULLS LAST', 'PARTITION BY'],
    optionExplanations: [
      { value: 'NTILE', explanation: 'NTILE(n)은 결과를 n개의 동일한 크기 그룹으로 나눕니다' },
      { value: 'ROW_NUMBER', explanation: '순번을 매기는 함수입니다. 그룹 분류와는 다릅니다' },
      { value: 'RANK', explanation: '순위를 매기는 함수입니다. n개 그룹 분류와는 다릅니다' },
      { value: 'DENSE_RANK', explanation: '순위를 매기는 함수입니다. n개 그룹 분류와는 다릅니다' },
      { value: 'DESC', explanation: '내림차순 정렬입니다. 높은 경험치가 상위 그룹(1)이 되려면 DESC가 필요합니다' },
      { value: 'ASC', explanation: '오름차순 정렬입니다. 낮은 경험치가 그룹 1이 됩니다' },
      { value: 'NULLS LAST', explanation: 'NULL 값을 마지막에 배치합니다. 정렬 방향 자체가 아닙니다' },
      { value: 'PARTITION BY', explanation: '윈도우를 나누는 키워드입니다. 전체를 4분위로 나눌 때는 PARTITION이 아닌 ORDER만 필요합니다' },
    ],
    correctAnswers: ['NTILE', 'DESC'],
    hints: {
      directional: ['결과를 동일한 크기의 N개 그룹으로 나누는 함수를 선택하세요'],
      constraint: ['상위 그룹이 1이 되려면 높은 경험치가 먼저 와야 합니다. 어떤 정렬 방향인가요?'],
      misconception: ['NTILE(4)는 데이터를 4개 등급으로 나눕니다. RANK나 ROW_NUMBER와 목적이 다릅니다'],
    },
  } as FillProblem,
  {
    id: 'swf25',
    category: 'sql-window',
    difficulty: 'intermediate',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '광고별 가장 최근 이벤트 유형 각 행에 표시하기',
    question: '광고 로그 테이블에서 광고별로 가장 최근에 발생한 이벤트 유형을 각 행에 함께 표시하는 SQL의 빈칸을 채우세요.',
    learningPoint: 'LAST_VALUE()는 프레임 끝 행의 값을 반환하며, ROWS BETWEEN으로 전체 파티션을 지정해야 한다',
    tags: ['LAST_VALUE', 'OVER', 'ROWS BETWEEN', 'UNBOUNDED FOLLOWING'],
    explanation: 'LAST_VALUE는 기본 프레임이 CURRENT ROW까지이므로 ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING을 명시해야 진짜 마지막 값을 얻을 수 있습니다.',
    relatedConceptTags: ['window-function'],
    sqlTemplate: 'SELECT ad_id, event_type, created_at,\n  ___(event_type) OVER (\n    PARTITION BY ad_id\n    ORDER BY created_at\n    ROWS BETWEEN UNBOUNDED PRECEDING AND ___\n  ) AS latest_event\nFROM ad_logs;',
    blanks: 2,
    options: ['LAST_VALUE', 'FIRST_VALUE', 'LEAD', 'LAG', 'UNBOUNDED FOLLOWING', 'CURRENT ROW', '1 FOLLOWING', 'UNBOUNDED PRECEDING'],
    optionExplanations: [
      { value: 'LAST_VALUE', explanation: '프레임 내 마지막 값을 반환합니다. UNBOUNDED FOLLOWING과 함께 써야 진짜 마지막 값을 얻습니다' },
      { value: 'FIRST_VALUE', explanation: '프레임 내 첫 번째 값을 반환합니다. 마지막 값과 반대입니다' },
      { value: 'LEAD', explanation: '다음 행의 값을 가져옵니다. 마지막 행 값과는 다른 개념입니다' },
      { value: 'LAG', explanation: '이전 행의 값을 가져옵니다. 마지막 행 값이 아닙니다' },
      { value: 'UNBOUNDED FOLLOWING', explanation: '파티션의 끝까지를 의미합니다. LAST_VALUE가 진짜 마지막 값을 반환하도록 합니다' },
      { value: 'CURRENT ROW', explanation: '현재 행까지만을 의미합니다. LAST_VALUE와 함께 쓰면 현재 행이 마지막이 됩니다' },
      { value: '1 FOLLOWING', explanation: '현재 행 다음 1행까지를 의미합니다. 전체 파티션의 마지막이 아닙니다' },
      { value: 'UNBOUNDED PRECEDING', explanation: '파티션의 시작부터를 의미합니다. ROWS BETWEEN의 시작 부분에 이미 있습니다' },
    ],
    correctAnswers: ['LAST_VALUE', 'UNBOUNDED FOLLOWING'],
    hints: {
      directional: ['파티션 내 마지막 값을 반환하는 함수를 선택하세요'],
      constraint: ['LAST_VALUE가 진짜 마지막 값을 보려면 프레임 끝을 어디까지 지정해야 할까요?'],
      misconception: ['LAST_VALUE는 기본 프레임(CURRENT ROW까지)만 보면 현재 행 값만 반환합니다. 반드시 UNBOUNDED FOLLOWING이 필요합니다'],
    },
  } as FillProblem,
  {
    id: 'swf26',
    category: 'sql-window',
    difficulty: 'intermediate',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '구독 사용자별 최근 3개 구독 금액 이동 평균 계산',
    question: '구독 테이블에서 각 사용자의 현재 행을 포함한 최근 3개 구독(현재+이전2) 금액의 이동 평균을 구하는 SQL의 빈칸을 채우세요.',
    learningPoint: 'ROWS BETWEEN 2 PRECEDING AND CURRENT ROW로 이동 평균을 계산한다',
    tags: ['AVG', 'OVER', 'ROWS BETWEEN', 'PRECEDING'],
    explanation: 'ROWS BETWEEN 2 PRECEDING AND CURRENT ROW는 현재 행 포함 최근 3행(2 앞 + 현재)을 프레임으로 지정합니다. 이를 이용해 이동 평균을 구할 수 있습니다.',
    relatedConceptTags: ['window-function'],
    sqlTemplate: 'SELECT user_id, start_date, amount,\n  AVG(amount) OVER (\n    PARTITION BY user_id\n    ORDER BY start_date\n    ___ BETWEEN 2 ___ AND CURRENT ROW\n  ) AS moving_avg_3\nFROM subscriptions;',
    blanks: 2,
    options: ['ROWS', 'RANGE', 'GROUPS', 'PRECEDING', 'FOLLOWING', 'CURRENT ROW', 'UNBOUNDED', 'PARTITION'],
    optionExplanations: [
      { value: 'ROWS', explanation: '물리적 행 수를 기준으로 프레임을 지정합니다. 이동 평균에 가장 적합합니다' },
      { value: 'RANGE', explanation: '값의 범위를 기준으로 프레임을 지정합니다. ROWS와 결과가 다를 수 있습니다' },
      { value: 'GROUPS', explanation: 'ORDER BY 값이 같은 그룹 수를 기준으로 합니다. 일반적이지 않은 옵션입니다' },
      { value: 'PRECEDING', explanation: '현재 행 이전을 의미합니다. 2 PRECEDING은 2행 앞까지를 나타냅니다' },
      { value: 'FOLLOWING', explanation: '현재 행 이후를 의미합니다. 이전 2행이 아닌 다음 행을 지정할 때 사용합니다' },
      { value: 'CURRENT ROW', explanation: '현재 행을 나타냅니다. 이미 AND 뒤에 있습니다' },
      { value: 'UNBOUNDED', explanation: '제한 없이 처음 또는 끝까지를 의미합니다. 2행 앞과는 다릅니다' },
      { value: 'PARTITION', explanation: 'PARTITION BY의 일부입니다. 프레임 지정과는 다릅니다' },
    ],
    correctAnswers: ['ROWS', 'PRECEDING'],
    hints: {
      directional: ['물리적 행 수 기준으로 프레임을 지정하는 키워드를 선택하세요'],
      constraint: ['2 ___ 는 현재 행 기준 2행 앞을 의미합니다. 빈칸에 들어갈 방향 키워드는 무엇인가요?'],
      misconception: ['RANGE는 값 범위 기반이고, ROWS는 행 수 기반입니다. 이동 평균에는 ROWS가 더 직관적입니다'],
    },
  } as FillProblem,
  {
    id: 'swf27',
    category: 'sql-window',
    difficulty: 'advanced',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '배달 레스토랑별 누적 최고 평점 추적하기',
    question: '배달 테이블에서 레스토랑별로 배달 완료 순서에 따른 누적 최고 평점과 현재 평점의 차이를 구하는 SQL의 빈칸을 채우세요.',
    learningPoint: 'MAX() OVER + ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW로 누적 최대값을 추적한다',
    tags: ['MAX', 'OVER', 'ROWS BETWEEN', 'UNBOUNDED PRECEDING'],
    explanation: 'MAX(rating) OVER (PARTITION BY restaurant_id ORDER BY delivered_at ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)는 현재까지의 누적 최고 평점을 구합니다.',
    relatedConceptTags: ['window-function'],
    sqlTemplate: 'SELECT order_id, restaurant_id, rating, delivered_at,\n  ___(rating) OVER (\n    PARTITION BY restaurant_id\n    ORDER BY delivered_at\n    ROWS BETWEEN ___ AND CURRENT ROW\n  ) AS cumulative_max_rating\nFROM deliveries;',
    blanks: 2,
    options: ['MAX', 'MIN', 'AVG', 'FIRST_VALUE', 'UNBOUNDED PRECEDING', 'CURRENT ROW', '1 PRECEDING', 'UNBOUNDED FOLLOWING'],
    optionExplanations: [
      { value: 'MAX', explanation: '최대값을 반환합니다. 누적 최고 평점 추적에 적합합니다' },
      { value: 'MIN', explanation: '최소값을 반환합니다. 최고 평점이 아닌 최저 평점을 구합니다' },
      { value: 'AVG', explanation: '평균을 반환합니다. 최고값 추적과는 다릅니다' },
      { value: 'FIRST_VALUE', explanation: '첫 번째 값을 반환합니다. 누적 최대값과는 다릅니다' },
      { value: 'UNBOUNDED PRECEDING', explanation: '파티션 시작부터 현재 행까지의 프레임을 지정합니다. 누적 집계에 필수입니다' },
      { value: 'CURRENT ROW', explanation: '현재 행까지를 의미합니다. 이미 AND 뒤에 있습니다' },
      { value: '1 PRECEDING', explanation: '1행 앞부터만을 의미합니다. 전체 누적이 아닌 직전 1행만 포함됩니다' },
      { value: 'UNBOUNDED FOLLOWING', explanation: '파티션 끝까지를 의미합니다. 누적 최대값 계산에는 필요하지 않습니다' },
    ],
    correctAnswers: ['MAX', 'UNBOUNDED PRECEDING'],
    hints: {
      directional: ['누적 최대값을 구하는 집계 함수를 선택하세요'],
      constraint: ['파티션의 처음부터 현재 행까지의 프레임을 지정하는 표현을 선택하세요'],
      misconception: ['FIRST_VALUE는 첫 번째 값만 반환하지만, MAX는 누적 최대값을 추적합니다'],
    },
  } as FillProblem,
  {
    id: 'swf28',
    category: 'sql-window',
    difficulty: 'advanced',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '게임 점수 상위 10% 구간 CTE로 필터링하기',
    question: '게임 점수 테이블에서 NTILE을 활용해 점수 기준 상위 10% 구간(1분위)에 속하는 기록만 조회하는 SQL의 빈칸을 채우세요.',
    learningPoint: 'NTILE(10)으로 10분위를 나누고 CTE로 필터링한다',
    tags: ['NTILE', 'OVER', 'ORDER BY', 'CTE'],
    explanation: 'NTILE(10) OVER (ORDER BY score DESC)로 전체를 10개 그룹으로 나누고, 1분위(최상위)만 WHERE 조건으로 필터링합니다. 윈도우 함수 결과는 WHERE에서 직접 쓸 수 없어 CTE가 필요합니다.',
    relatedConceptTags: ['window-function'],
    sqlTemplate: 'WITH ranked AS (\n  SELECT user_id, score, played_at,\n    NTILE(10) OVER (ORDER BY score DESC) AS score_decile\n  FROM scores\n)\nSELECT user_id, score, played_at\nFROM ranked\n___ score_decile = ___;',
    blanks: 2,
    options: ['WHERE', 'HAVING', 'FILTER', 'ON', '1', '10', '0', 'NULL'],
    optionExplanations: [
      { value: 'WHERE', explanation: 'CTE 결과를 필터링합니다. 윈도우 함수 결과를 걸러낼 때는 CTE + WHERE를 사용합니다' },
      { value: 'HAVING', explanation: 'GROUP BY 결과를 필터링합니다. CTE 단순 조회에는 WHERE가 적합합니다' },
      { value: 'FILTER', explanation: 'SQL에서 FILTER는 집계 함수와 함께 쓰는 키워드입니다. 행 필터링에는 WHERE를 씁니다' },
      { value: 'ON', explanation: 'JOIN 조건에 쓰는 키워드입니다. 행 필터링과는 다릅니다' },
      { value: '1', explanation: 'NTILE(10)에서 1분위는 상위 10%를 의미합니다. score_decile = 1이 상위 그룹입니다' },
      { value: '10', explanation: 'NTILE(10)에서 10분위는 하위 10%를 의미합니다. 상위가 아닙니다' },
      { value: '0', explanation: 'NTILE의 결과는 1부터 시작합니다. 0은 존재하지 않습니다' },
      { value: 'NULL', explanation: 'NTILE 결과는 숫자입니다. NULL로 필터링하는 것은 의미 없습니다' },
    ],
    correctAnswers: ['WHERE', '1'],
    hints: {
      directional: ['CTE 결과에서 특정 조건의 행만 가져오는 SQL 키워드는 무엇인가요?'],
      constraint: ['NTILE(10)에서 가장 높은 그룹 번호는 1입니다. 상위 10%는 몇 분위인가요?'],
      misconception: ['윈도우 함수 결과는 WHERE 절에서 직접 참조할 수 없어서 CTE나 서브쿼리가 필요합니다'],
    },
  } as FillProblem,
  {
    id: 'swf29',
    category: 'sql-window',
    difficulty: 'advanced',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '스트리밍 연속 시청 세션 감지를 위한 이전 값 조회',
    question: '시청 로그 테이블에서 사용자별로 이전 시청 시작 시각과 이전 시청 길이를 조회해 연속 시청 여부 판단에 사용하는 SQL의 빈칸을 채우세요.',
    learningPoint: 'LAG()로 이전 행 값을 가져와 시간 차이 계산으로 연속 이벤트를 감지한다',
    tags: ['LAG', 'OVER', 'PARTITION BY', 'ORDER BY'],
    explanation: 'LAG(started_at) OVER (PARTITION BY user_id ORDER BY started_at)로 이전 시청 시작 시각을, LAG(duration_sec)로 이전 시청 길이를 가져옵니다. 두 값으로 이전 시청 종료 시각을 계산해 연속 여부를 판단합니다.',
    relatedConceptTags: ['window-function'],
    sqlTemplate: 'SELECT user_id, content_id, started_at, duration_sec,\n  ___(started_at) OVER (PARTITION BY user_id ORDER BY started_at) AS prev_start,\n  ___(duration_sec) OVER (PARTITION BY user_id ORDER BY started_at) AS prev_duration\nFROM watch_logs;',
    blanks: 2,
    options: ['LAG', 'LEAD', 'FIRST_VALUE', 'LAST_VALUE', 'SUM', 'AVG', 'ROW_NUMBER', 'NTILE'],
    optionExplanations: [
      { value: 'LAG', explanation: '이전 행의 값을 가져옵니다. 연속 시청 감지를 위해 이전 started_at과 duration_sec이 모두 필요합니다' },
      { value: 'LEAD', explanation: '다음 행의 값을 가져옵니다. 이전 시청 정보가 아닌 다음 시청 정보를 가져옵니다' },
      { value: 'FIRST_VALUE', explanation: '파티션 첫 번째 값을 반환합니다. 바로 이전 행 값과는 다릅니다' },
      { value: 'LAST_VALUE', explanation: '파티션 마지막 값을 반환합니다. 이전 행 값이 아닙니다' },
      { value: 'SUM', explanation: '합계를 구합니다. 이전 행 값을 가져오는 용도와 다릅니다' },
      { value: 'AVG', explanation: '평균을 구합니다. 이전 행 값을 가져오는 용도와 다릅니다' },
      { value: 'ROW_NUMBER', explanation: '순번을 부여합니다. 이전 행 값 조회와는 관계가 없습니다' },
      { value: 'NTILE', explanation: 'N분위로 나눕니다. 이전 행 값 조회와는 관계가 없습니다' },
    ],
    correctAnswers: ['LAG', 'LAG'],
    hints: {
      directional: ['이전 행의 값을 가져오는 함수를 선택하세요. 두 빈칸 모두 같은 함수입니다'],
      constraint: ['started_at과 duration_sec 모두 이전 행의 값이 필요합니다'],
      misconception: ['LEAD는 다음 행, LAG는 이전 행입니다. 연속 시청 감지는 이전 시청 정보가 필요합니다'],
    },
  } as FillProblem,
  {
    id: 'swf30',
    category: 'sql-window',
    difficulty: 'advanced',
    type: 'fill',
    availableModes: ['fill'] as const,
    title: '이커머스 주문 금액 전체 대비 비율과 누적 비율 계산',
    question: '주문 테이블에서 각 주문 금액이 전체 주문 합계 대비 비율(%)과 누적 비율을 함께 구하는 SQL의 빈칸을 채우세요.',
    learningPoint: 'SUM() OVER ()로 전체 합계를, SUM() OVER (ORDER BY ...)로 누적 합계를 구해 비율을 계산한다',
    tags: ['SUM', 'OVER', 'ORDER BY', 'ROWS BETWEEN'],
    explanation: 'SUM(amount) OVER ()는 파티션 없이 전체 합계를 반환합니다. SUM(amount) OVER (ORDER BY created_at)는 created_at 순서로 누적 합계를 계산합니다. 각 행의 amount를 전체/누적 합계로 나눠 비율을 구합니다.',
    relatedConceptTags: ['window-function'],
    sqlTemplate: 'SELECT order_id, user_id, amount, created_at,\n  ROUND(amount * 100.0 / SUM(amount) ___(___)  , 2) AS pct_of_total,\n  ROUND(SUM(amount) OVER (ORDER BY created_at\n    ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n  ) * 100.0 / SUM(amount) OVER (), 2) AS cumulative_pct\nFROM orders;',
    blanks: 2,
    options: ['OVER', 'PARTITION BY', 'ORDER BY', 'ROWS BETWEEN', '()', 'user_id', 'created_at', 'UNBOUNDED PRECEDING'],
    optionExplanations: [
      { value: 'OVER', explanation: '윈도우 함수를 정의하는 키워드입니다. SUM(amount) 뒤에 반드시 필요합니다' },
      { value: 'PARTITION BY', explanation: '파티션 분할 키워드입니다. 전체 합계는 파티션 없이 OVER ()를 씁니다' },
      { value: 'ORDER BY', explanation: '정렬 기준을 지정합니다. 전체 합계용 OVER에는 ORDER BY가 불필요합니다' },
      { value: 'ROWS BETWEEN', explanation: '프레임 지정 키워드입니다. 전체 합계에는 프레임이 필요하지 않습니다' },
      { value: '()', explanation: '빈 괄호는 전체 결과셋을 대상으로 집계함을 의미합니다. SUM(amount) OVER ()는 전체 합계입니다' },
      { value: 'user_id', explanation: '사용자 ID입니다. 전체 합계에는 파티션이나 정렬이 필요 없습니다' },
      { value: 'created_at', explanation: '주문 날짜입니다. 전체 합계 계산에 ORDER BY는 불필요합니다' },
      { value: 'UNBOUNDED PRECEDING', explanation: '누적 프레임 시작점입니다. 전체 합계용 OVER 내부에는 해당 없습니다' },
    ],
    correctAnswers: ['OVER', '()'],
    hints: {
      directional: ['SUM(amount) ___(___) 에서 윈도우 함수를 시작하는 키워드와 전체 집계를 의미하는 표현을 찾으세요'],
      constraint: ['파티션이나 ORDER BY 없는 OVER ()는 전체 결과셋을 대상으로 합계를 구합니다'],
      misconception: ['OVER (ORDER BY ...)는 누적 집계를 만듭니다. 전체 합계는 OVER ()로 파티션과 ORDER BY 없이 써야 합니다'],
    },
  } as FillProblem,
];
